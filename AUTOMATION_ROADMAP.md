# AURA — Automation Roadmap (Live)

> **역할**: 현재 진행 중인 Phase/Step 의 라이브 상태 + 핸드오프 로그.
> **권위**: rules.md §11.1 — Live canonical (Tasks.md 는 release snapshot, ROADMAP 우선).
> **업데이트 주기**: 세션 단위 또는 Phase 전이 시점.

---

## 현재 상태 (2026-05-06)

> **2026-05-03 Pivot**: 자체 RE 엔진 (in-house disassembler / parser / decompiler /
> symbolic / Qt6 GUI / collab server) 전량 폐기. 외부 도구 오케스트레이션 +
> 새 GUI/UX 방향으로 재출발. 작업 브랜치: `feat/strip-legacy-engines`.
>
> **2026-05-06 Runtime baseline update (ADR-0052)**: 신규 실행/시연 기준의
> mandatory reference engine 은 fetch script 가 내려받아 검증한 **Rizin
> 0.8.0 shared64**. 기본 path =
> `third_party/rizin/0.8.0-shared/rizin-win-installer-clang_cl-64/bin/rizin.exe`,
> 이지만 추출된 바이너리는 Git 에 커밋하지 않는다. rz-ghidra 0.8.0 은
> 설치되어 있을 때 preferred pseudo-C provider 이며 `SLEIGHHOME` =
> `third_party/rizin/0.8.0-shared/rizin-win-installer-clang_cl-64/lib/rizin/plugins/rz_ghidra_sleigh`.
> fetch/install script 가 실제 설치하기 전까지 자동 설치 보장으로 설명하지
> 않는다. 부재 시 deterministic install guidance 를 표시하고 pseudo-C 를
> 조작해 만들지 않는다.
> 0.8.2 계열은 과거 analyze 정합화 기록으로만 취급하며 신규 기준이 아니다.
> Defender/SmartScreen/EDR 의 cyber security risk 경고는 RE 도구 특성상
> 예상 가능한 경고로 문서화하고, 코드 우회는 금지한다.

| Phase | 제목 | 상태 | 위치 |
|-------|------|------|------|
| 1 | Engine Adapter Contract / Orchestrator | ✅ closed (보존) | Tasks.md §Phase 1 |
| 2 | Rizin / Ghidra Adapters | ✅ closed (보존) | Tasks.md §Phase 2 |
| **2.5** | **Probe / Bootstrap / Consent** | ✅ **closed** (Slices 1-60 코어 + closeout 2.5.1 (X.1-X.7) + lookup contract 2.5.2 (X.8-X.8.2) + build infra fix. **probe 58 + subprocess 9 + consent 15 + bootstrap 17 = 99 cases GREEN, ctest 5/5 unit+integration GREEN (probe + subprocess + consent + bootstrap + probe_engines_smoke)**. Production: `aura --probe-engines` CLI subcommand (envelope JSON), real subprocess AuraCommandRunner (POSIX poll/drain + Windows reader threads, deadlock 가드), env→vendored→PATH lookup contract (5 env vars), Windows MSVC 한글 경로 빌드 GREEN. Codex 5-round adversarial review cycle (각 round 별 high finding 닫힘 — 최종 verdict APPROVE). §1.6 동시성 primitives 만 Phase 12.1 SessionManager 통합 보류 (ADR-0051 phase 번호 재배정).) | main |
| 3 | Unified Model Ingestion | ✅ closed (보존) | Tasks.md §Phase 3 |
| 4 | Override Layer | ✅ closed (보존) | Tasks.md §Phase 4 |
| 5 | (구) GUI Integration | ❌ scrapped — 신규 GUI 로 재정의 | — |
| 6 | (구) Secondary Engine Adapters | ⏸️ deferred | — |
| 7 | (구 번호) Multi-engine Compare | ⏸️ deferred, 목표 보존 | Future Phase: Multi-decompiler serving |
| 8 | (구) Performance / mmap | ⏸️ deferred | — |
| 9 | Platform Coverage (Windows MSVC) | ✅ closed — issue 01~08 closed sweep, issue 09 (CI runner) 흡수: orchestrator-smoke.yml 의 windows job (commit `0070b985`) + Phase 11.1 cli_smoke 추가. Phase 9 자산 superseded by Phase 10/11.1. | — |
| **10** | **Legacy strip + orchestrator_smoke gate** | 🟢 **landed** (commits `0483147f`..`0070b985`) | main |
| **11.1** | **CLI v1 — `analyze` / `info` / `llm-context`** | 🟢 **landed** (commits `c6596be1`..`e61fa879`, ctest -L integration 2/2 GREEN) | main |
| 11.1.1 | minor patch: `aura engines` + cli_smoke failure injection | ⏸️ planned | — |
| 11.2 | LLM 통합 e2e cookbook | 🟡 **in progress** — 기존 LM Studio/qwen transcript 는 과거 단일 LLM 호출 검증으로 보존. 현재 Phase 13 기준 Privacy Filter/MCP/Token Classification/Rule Pack end-to-end cookbook 은 미완이며, GUI IPC 자동화 경로만 검증 완료. | Phase 13 |
| **11.3** | **GUI v1 — project-first 흐름** | 🟢 **landed** (ADR-0035, Slice A~H, ctest -L integration 3/3 GREEN: orchestrator_smoke + cli_smoke + gui_smoke) | main |
| **11.3.1** | **GUI v1.1 — Cutter-style workspace + Decompile pane** | 🟢 **landed** (ADR-0036, Slice A~F, ctest -L integration 3/3 GREEN, decompile plugin missing 시 install-guidance placeholder 정상) | main |
| **11.3.1.1** | **GUI v1.1 patch — PDF disasm fallback** | 🟢 **landed** (ADR-0036 amend, AURA_RIZIN_DEC_PDF=4 backend, scr.color=0/utf8=0, Easy_CrackMe.exe 실증 — Ea5yR3versing 추출) | main |
| **11.3.2** | **GUI v1.2 — Override Editor (rename / reset)** | 🟢 **landed** (ADR-0037, Slice A~H, ctest -L integration 3/3 GREEN, rename round-trip 검증 — Easy_CrackMe IPC 도입 후 자동 검증 예정) | main |
| **11.3.3** | **GUI RPC — TCP localhost JSON-line server** | 🟢 **landed** (ADR-0038, Slice A-D, batch decompile 11.3.4 후속 포함) | main |
| 11.3.4 | Batch decompile — single Rizin invocation, eager cache fill | 🟢 **landed** (commit `0889d22c`) | main |
| **11.3.5** | **Xrefs / Symbols / Imports docks (cycle 1~7)** | 🟢 **landed** (commits `2d13551f`..`c39f3683`, ctest -L integration 3/3 GREEN at land time) | main |
| **11.3.6** | **Strings dock — AuraStringRecord + body schema v3** | 🟢 **landed** (ADR-0039, cycles 1~4: 9th 1급 record + body v3 + izj parsing + CLI emitter + RPC list_strings + GUI Strings dock. ctest -L integration: orchestrator + gui GREEN, cli 14/15 incl. pre-existing baseline fail) | claude/vigorous-panini-aa1b33 |
| **11.3.7** | **(P2.F2) Disassembly view — instruction-level** | 🟢 **landed** (ADR-0040, cycles 1~4: InstructionRecord+DisasmBody / pdfj parsing+CLI / DisasmPane GUI+RPC+tabify / line_map one-way sync. ctest -L integration: orchestrator + gui GREEN, cli 15/16 incl. pre-existing baseline fail) | claude/vigorous-panini-aa1b33 |
| **11.3.8** | **(P2.F3) CFG view — control flow graph 시각화** | 🟢 **landed** (ADR-0041, cycles 1~4: ADR + skeleton / BasicBlockNode+EdgeArrow+BFS / pan+zoom+double-click cross-pane sync. **신규 record/body 0** — analyze body 의 blocks[]/edges[] 재사용. ctest -L integration: orchestrator + gui GREEN, cli 16/17 incl. pre-existing baseline fail) | claude/vigorous-panini-aa1b33 |
| **11.3.9** | **(P2.F4) Decompile pane 강화 (양방향 sync + context menu + nav history)** | 🟢 **landed** (ADR-0042, cycles 1~3: bidirectional sync / 5 context-menu actions (ANNOTATION schema 재사용) / nav history (QStack + Alt+Left/Right + View 메뉴). C4 polish 는 선택 — 별도 commit 없이 close-out 표기. ctest -L integration: orchestrator + gui GREEN, cli 16/17 incl. pre-existing baseline fail.) | claude/vigorous-panini-aa1b33 |
| **11.3.10** | **(P2.F5) Hex view dock — raw bytes 표시** | 🟢 **landed** (ADR-0043, cycles 1~4: ADR + skeleton+mmap+기본 렌더 / window paging via scrollToAddr / cross-pane sync (disasm.instructionSelected & cfg.blockActivated → hex.scrollToAddr). **P2 (기초 RE 기능) 전체 종료** — F2/F3/F4/F5 모두 closed. ctest -L integration: orchestrator + gui GREEN, cli 16/17 incl. pre-existing baseline fail.) | claude/vigorous-panini-aa1b33 |
| **11.3.11** | **GUI tree-browser polish — Cutter-style left browsers** | 🟢 **landed local** (2026-05-06 사용자 승인. 기본 workspace 를 좌측 상단 Functions/Xrefs, 좌측 하단 Strings/Symbols/Imports, 우측 Decompile/Disassembly/Full Disassembly/CFG/Hex 로 재배치. Functions/Xrefs/Symbols/Imports/Strings 를 `QTreeView` 로 통일하고 parent row + metadata child rows 로 표시. 미제공 metadata 는 `-` 로 표시하며 GUI 가 값을 합성하지 않음. Verification: `aura-gui` build GREEN, `gui_smoke` GREEN.) | local/Codex |
| **11.3.12** | **Full Disassembly continuous listing tab** | 🟢 **landed local** (2026-05-06 사용자 승인. 기존 함수 단위 Disassembly 탭은 유지하고, 별도 Full Disassembly 탭을 추가. Cutter처럼 연속 VA listing 을 보여주고 함수 목록 선택 시 해당 주소로 스크롤/하이라이트. 내부 호출은 Rizin `pdj <count>` structured stream + bounded `pD 4096` arrow text 를 함께 사용하며, `Structured` 탭은 `pD` mixed listing 을 분류해 invalid/data/comment/label 줄도 보존. Chunk cache / lazy paging 은 미구현으로 후속 개선.) | local/Codex |
| 11.4 | `override` CLI (list/get/put/delete + --no-analyze) | 🟢 **landed** (commit `2eb59952`, ctest -L integration 2/2 GREEN) | main |
| 11.4.1 | 패키징 + cmake --install 검증 | 🟢 **landed** (cycle-22 2026-05-04: install prefix 에 bin/aura{,-gui}.exe + include/aura/*.h + share/doc/aura/README+LICENSE 정상 배포, installed `aura --version` GREEN) | claude/vigorous-panini-aa1b33 |
| **11.4.2** | **(P4.PP4) Auto-labelling — candidate suggestion** | 🟢 **landed v1 (heuristic)** (ADR-0044, cycles 1~3: ADR + CLI label-candidates + GUI Suggest-labels popover. **C4 LLM source v2 = deferred** — network/API 비결정성으로 ctest 검증 어려움, ADR-0033 "AURA core 무 LLM" 보존 위해 GUI 통합 별도 phase 분리. heuristic v1 만으로 PP4 의 80% user-value 달성 (ADR-0044 D1).) | claude/vigorous-panini-aa1b33 |
| **11.4.3** | **(P4.PP1) Type Propagation — variable type 연쇄 전파 후보** | 🟢 **landed v1 (display-only)** (ADR-0045, cycles 1~5: ADR + algorithm module + CLI propagate-type + GUI Propagate-type popover + Apply stub. Variable-target store_put 통합은 별도 "PP1 v2 store wire" phase 분리 (buildKeyForVariable 신설 필요). ctest -L integration: orchestrator + gui GREEN, cli 18/19. **3번째 display-only path 적용 — R-9 boundary "lookup OK / inference X" 패턴 안정 정착**.) | claude/vigorous-panini-aa1b33 |
| **11.4.3-v2** | **(P4.PP1) PP1 store wire — Type propagation DB apply** | 🟢 **landed** (2026-05-06, D-35 승인. 기존 11.4.3 의 “PP1 v2 store wire” 분리 항목을 닫음. CLI `propagate-type --apply --all/--candidate-ids --project` 가 선택 candidate 를 기존 SQLite `override_store` 의 `AURA_OVERRIDE_PAYLOAD_TYPE` row 로 저장. 분석 결과 전체 DB 캐시 / candidate cache table 신설 없음. Verification: `cmake --build build-rel --config Release` GREEN, `ctest --test-dir build-rel -C Release --output-on-failure` 7/7 GREEN.) | local/Codex |
| **11.4.4** | **(P4.PP2) Struct Field Misidentification — offset cluster + 사용자 선택** | 🟢 **landed v1 (display-only)** (ADR-0046, cycles 1~3: ADR + CLI field-candidates (stack cluster) + GUI Suggest-fields popover. (a) op_str regex source = v2 deferred (DISASM body 통합 필요), struct 정의 합성 = R-9 위반 영구 거부. **Display-only path 4번째 적용 — 패턴 stable**.) | claude/vigorous-panini-aa1b33 |
| **11.4.5** | **(P4.PP3) Array/Pointer Expression — stride raw stat + 사용자 선택** | 🟢 **landed v1 (display-only)** (ADR-0047, cycles 34~37: ADR + CLI array-candidates (b)+(c) + cli_smoke naming-hygiene guard + GUI Suggest-array-shape popover w/ (a) op_str source GUI-only via in-process disasmList cache + formatArrayCandidateRow extracted + gui_smoke synthetic-fixture naming-hygiene regression guard (forbid "stride"/"array"/"element_type" in AURA-synthesized text, engine-verbatim typeStr stripped before scan). **PP3 closed → 사용자 plan v2 의 RE 도구 차별화 4종 (PP4 + PP1 + PP2 + PP3) ALL CLOSED v1.** ctest -L integration: orchestrator + gui GREEN, cli 20/21 (1 RED 동일 pre-existing).) | claude/vigorous-panini-aa1b33 |
| **11.5** | **P5 polish suite — keyboard / navigation / shortcuts** | 🟢 **landed** (cycles 39~47, 8 features: Recent Projects MRU + drag-drop binary + Go-to-address (Ctrl+G) + Find function (Ctrl+F) + Copy address/name + pane font zoom (Ctrl+= / Ctrl+- / Ctrl+0) + Keyboard shortcuts cheat sheet (Ctrl+/) + Dock focus shortcuts (Ctrl+1/2/3/4). All shortcuts documented via `keyboardShortcutsHtml()` single source of truth + gui_smoke regression guard. R-1/R-9/R-10/R-12 위반 0.) | main |
| **11.6** | **Disasm flow arrow gutter (Cutter-style)** | 🟢 **landed** (ADR-0048, T1×3 + T2×4 + T3 = 8 commits `610ed33b`..`8eb6eacd`. T1 backend body+RPC arrow text / T2 DisasmFlowGutter widget + lane assignment + per-kind colours + spans-viewport fix / T3 hover highlight + click-to-navigate. Land-time ctest -L integration 3/3 GREEN; closeout local re-verify (`AURA_BUILD_GUI=OFF`) 2/2 GREEN. R-9 위반 0 — 기존 `pdj.type/jump/fail` 만 재투영, 신규 record 0.) | main |
| **13.0a** | **GUI IPC attach path — `aura gui ...` client mode** | 🟢 **landed local** (2026-05-06 사용자 승인. `aura-gui --rpc-port` 로 열린 localhost JSON-line RPC 에 나중에 실행한 `aura gui status/open-project/add-binary/analyze/functions/decompile/strings/symbols/xrefs/quit` 가 접속. RPC `status` method 추가. MCP 는 GUI 내장이 아니라 별도 `aura-mcp` Privacy Filter & Policy Gateway 로 계획 유지. Gateway 는 두 번째 LLM 이 아니라 deterministic policy layer. Verification: live IPC round-trip `status → open-project → add-binary → analyze → functions → decompile` GREEN, `gui_smoke` GREEN, build-rel ctest 7/7 GREEN.) | local/Codex |

상태 기호: ⏸️ planned/paused · 🟡 active · 🟢 landed · ✅ closed · ❌ scrapped

---

## 다음 세션 백로그 (2026-05-05 저녁 — Phase 2.5 종료 직후 식별)

> 본 세션에서 완료 못 한 / 다음 세션 진입점 후보 정리. 우선순위는 사용자 결정.

### 단기 enhancement (~30-100줄, 1 cycle)

| ID | 작업 | 비용 | 근거 |
|----|------|------|------|
| **2.5.3 X.9** | install_hint 메시지에 env var 옵션 안내 추가 (예: "또는 `AURA_RIZIN_BIN=/path` 설정") | ~30줄, 5 probe 모두 + 테스트 1-2개 | ADR-0050 §Out of scope #1 |
| **9.11** | Windows MSVC CI smoke step — non-ASCII path (예: `$env:RUNNER_TEMP\aura-한글`) 에서 빌드 + `--probe-engines` + `probe_engines_smoke` | yml ~30줄 + Phase 9 Issue 추가 | Codex review #6 medium, ADR-0049/0050 §Out of scope CI |
| **CLI orch 통일** | `build_orchestrator` (Rizin only) 의 lookup 을 `aura_probe_resolve_binary` 와 통합. 별도 ADR 필요. | ~50줄 + 테스트 | ADR-0050 §Out of scope #2 |

### 중기 phase (별 phase 단위, 다중 cycle)

> **Phase 번호 갱신 (2026-05-05, ADR-0051)**: plan 의 옛 11.5~11.10 = 코드베이스 의 Phase 11.5/11.6 (이미 ✅ landed) 와 충돌. 묶음 재배정 → **Phase 12 (12.1~12.6)** 로 이전. 옛 plan 12 → **Phase 13**.

| ID | 작업 | 의존 | 근거 |
|----|------|------|------|
| **Phase 2D** | ghidra-full opt-in vendoring + `AuraDaemon.java` (분석 결과 stdout JSON streaming) | Phase 2.5 ✅ | plan §2.1 + decompile co-primary 활성화 |
| **Phase 7.1** | angr `symbolic` request type 신설 — 외부 runner 방식 | Phase 2.5 ✅ + D-34 ✅ | plan §2.2. 첫 구현에서 `include/engine_request.h` enum/mask, manifest validation, orchestrator primary `{angr}`, raw+normalized symbolic result contract 갱신 필요. AURA 내부 symbolic execution 구현 금지. **주의**: 옛 Phase 7.1 (Decompile compare, Tasks_deferred) ≠ 신 7.1. |
| **Phase 12** (묶음) | **External Engine Session & Cache Layer** — 6 sub-phase | Phase 2.5 ✅ | plan §3-§5 |
| 12.1 | SessionManager (RAII + idle timer + LRU + 동시성 primitives) — Phase 2.5 §1.6 deferred 회수 | — | 옛 Plan 11.5 |
| 12.2 | Rizin r2pipe persistent session adapter | 12.1 | 옛 Plan 11.6 |
| 12.3 | Ghidra full + AuraDaemon.java + progressive readiness | 12.1, 2D | 옛 Plan 11.7 |
| 12.4 | DB 캐시 (analysis_hash + 2-source merge + field-level type) + invalidation | 12.1 | 옛 Plan 11.8 |
| 12.5 | dependents() with confidence pruning + expansion cap | 12.4 | 옛 Plan 11.9 |
| 12.6 | integrity check + crash recovery | 12.4 | 옛 Plan 11.10 |
| **Phase 13** | LLM Context Provider — single external/user-selected generative LLM + `aura-mcp` Token Classification/Rule Privacy Filter & Policy Gateway + GUI IPC/headless engine bridge | Phase 12 권장 | PRD D-39/D-41/D-42/D-43/D-44/D-45/D-46/D-47/D-48/D-49. 2개 생성형 LLM 상시 배치 금지. Privacy filter 는 문자열 기반 MVP 에서 시작해 Token Classification Model(`openai/privacy-filter` 기본 후보), 한국형/Secret Rule Pack, Eval Dataset 기반 평가를 결합한 민감정보 마스킹 시스템으로 확장한다. 2026-05-07 문자열 보호 DB 1차 기준 승인: String Alias(사용자 의미 별칭) 와 Mask Token(외부 전달 안전 토큰) 을 분리하고 `analysis_strings`, `string_overrides`, `string_protection_findings` 세 테이블로 시작한다. 2026-05-07 runtime asset registry 승인: 큰 모델/데이터셋은 `third_party/` 대신 `~/.aura/token-classification-models/`, `~/.aura/eval-datasets/` 에 두고 Rule Pack/Safety Profile 을 분리한다. 2026-05-07 Strings protection UI 승인: 별도 Safety 탭 대신 Strings 탭 최하단 child row 에 `기존`/`별칭`/`마스킹` 을 두고 원문은 기본 숨김. 2026-05-07 Safety core module layout 승인: 공용 로직은 `include/aura/safety/` + `src/core/safety/` 에 두고 GUI 는 표시만 담당. 2026-05-07 Strict MCP Coverage 승인: 새 analysis/export/automation surface 는 `aura-mcp` surface 까지 함께 갱신해야 완료다. MCP 는 GUI 화면이 아니라 Unified Model + Safety Core export-safe view 기준이며, schema envelope 은 engine-agnostic base record 를 유지하고 engine 별 차이는 `extensions.<engine_id>` 또는 raw artifact reference 로 격리한다. MCP 기본 tool 은 protected value 만 반환하고 raw 접근은 raw-access session toggle + 요청별 승인 + bounded scope + Raw Disclosure Event audit 가 필요한 privileged path 로만 허용한다. MCP 기능은 smoke test 가 아니라 MCP Security Regression Gate(schema/error envelope, protected default, raw non-disclosure, privileged denial, approval, bounded scope, audit, engine-agnostic schema, compatibility)를 통과해야 완료다. 최종 pipeline 은 Rule Pack Engine + Token Classification Model Adapter 가 원본 문자열을 각각 검사한 뒤 병합한다. 구현 순서는 Rule Pack 경로 우선이지만 모델 어댑터도 같은 Phase 13 목표 구현 대상이다. 내부 verifier 생성형 모델은 기본 요구사항에서 제외하고 별도 연구 phase 승인 시에만 재검토. |
| **Future Phase: Multi-decompiler serving** | Rizin/rz-ghidra + Ghidra + RetDec 등 복수 decompiler 결과를 같은 함수 단위로 동시 요청·보존·표시. display-only compare, merge/vote/자동 정정 금지. | Phase 6 RetDec 재개 + Phase 12 session/cache 권장 | PRD D-36 + Tasks_deferred Phase 7. 옛 "7.1 Decompile compare" 번호는 폐기됐지만 목표 자체는 살아 있음. 구체 CLI/GUI/cache schema 는 별도 ADR 필요. |

### 문서/거버넌스

| ID | 작업 | 비용 | 근거 |
|----|------|------|------|
| **ROADMAP 표 행 추가** | Phase 2.5.3 (X.9 시 신설), 2D, 7.1, 12 (12.1~12.6), 13 행 | 신규 phase 진입 시점 갱신 | 본 백로그가 그 자리 표시 — 진입 시 정식 행으로 promote |
| **PRD §F CLI feature 표** | `aura --probe-engines` 등 Phase 11.x CLI subcommand 누락 정리 (현 PRD 가 제대로 표 형태로 갖고 있는지 확인 필요) | 검토 1 cycle | 본 세션 미확인 |
| **사용자 메모리 갱신** | `workflow_preferences.md` 에 "Codex 5-round adversarial review 패턴" 정착 기록 (이번 세션 정착 사례) | ~5줄 | 본 세션 입증된 워크플로 |

### 인프라 (선택)

| ID | 작업 | 비고 |
|----|------|------|
| **Codex review 자동화 hook** | `.claude/settings.json` 에 commit 후 자동 `/codex:adversarial-review` 호출 hook | 사용자 결정 — auto 권장 vs 수동 트리거 |
| **`docs/agents/` 갱신** | 본 세션 정착된 `Codex 5-round review` 패턴을 `docs/agents/` 의 어느 파일에 기록할지 결정 | 신규 agent 문서 1개? |

### 차단 항목 / 의사결정 대기

- **CLI ADR 옵션 A 확정만 됨** — 옵션 B (별도 `aura-probe` 바이너리), C (영구 deferral) 는 영구 거부 처리 필요 — 별도 ADR amend 또는 PRD Open Decision close.
- **§1.6 동시성 도입 시점** — Phase 12.1 SessionManager 와 통합 명시됨. Phase 12.1 진입 시 `max_concurrent_sessions=3` 와 동일 primitive 공유 검증 필요.
- **Phase 7.1 symbolic** — D-34 로 정책 승인 완료. 구현 대기 상태. angr 외부 runner 방식만 허용하며 AURA 내부 symbolic execution 구현 금지.

### 진입 권장 순서

```
1. 2.5.3 X.9 (install_hint env var hint)        ← 가장 작음, 즉시 가능
2. CLI orch 통일                                ← 모순 잔여 해소
3. Phase 12.1 SessionManager                    ← §1.6 deferred 회수
4. Phase 2D OR Phase 7.1 구현                  ← 사용자 우선순위
5. Phase 12.2~12.6 (Rizin/Ghidra session, DB cache, dependents, integrity) ← 12.1 의존
6. Phase 13 (LLM context)                       ← 별도 긴 대화 권장 (사용자 메모리)
6. Phase 9 issue 09 + 11 (CI gate)              ← 시간 여유 시
```

---

## 활성 작업 — Phase 9 Platform Coverage

**시작**: 2026-05-03
**PRD**: [`.scratch/platform-coverage/PRD.md`](.scratch/platform-coverage/PRD.md)
**ADR**: [`docs/adr/0030-tier-platform-policy.md`](docs/adr/0030-tier-platform-policy.md)

### Issue 진행 상황

| # | 제목 | Status | depends-on |
|---|------|--------|------------|
| 01 | AuraStrBuf text buffer 도입 | **closed** (2026-05-03, TC-1~11 GREEN) | — |
| 02 | hir_emit.c 마이그레이션 → AuraStrBuf | **closed** (2026-05-03, Slice 1+2 RED-locks + 16/16 ctest GREEN) | 01 ✅ |
| 03a | tests/decompiler/* ir/hir_emit_pseudoc 마이그레이션 (13 callsite) | **closed** (2026-05-03, ir_emit_pseudoc_with_strbuf 신규) | 01 ✅ |
| 03b | tests/decompiler/* dwarf/pdb/exc/sr_emit 마이그레이션 (12 callsite) | **closed** (2026-05-03, 6 _with_strbuf adapter + aura_strbuf_capture_emit shared bridge) | 01 ✅, 03a ✅ |
| 04 | src/gui/* memstream → aura::StrBuf RAII (2 callsite) | **closed** (2026-05-03, file_loader.cpp + main_window.cpp) | 01 ✅ |
| 05 | aura_compat.h WEAK_STUB 매크로 | **closed** (2026-05-03, AURA_WEAK + WEAK_STUB alias) | — |
| 06 | tests/stubs MSVC LNK2005 제거 | **closed** (2026-05-03, #ifndef _MSC_VER guard) | 05 ✅ |
| 07 | MappedFile.fd 단일화 (G6) | **closed** (2026-05-03, header canonical + 10 guard 제거) | — |
| 08 | aura_compat 잔여 부채 | needs-triage | — |
| 09 | Windows MSVC CI runner | ready-for-human | 02·03·04·06·07 |
| 10 | Tier 1 enforcement gate | needs-info | 09 |

### 관련 점검 발견

선행 작업: Phase 1~4 재점검 ([`.scratch/phase-1-4-audit/summary.md`](.scratch/phase-1-4-audit/summary.md)) 에서 7건 결함 식별. issue 02·03·05·06·07 이 Phase 9 로 이관됨.

---

## 핸드오프 로그

| 날짜 | 완료 | 다음 | 차단 |
|------|------|------|------|
| 2026-05-05 (저녁) | **🎉 Phase 2.5.1 + 2.5.2 closeout patch — landed (10 commits, merged to main `65440808`)**. **Phase 2.5.1 (Slices X.1-X.7, 6 commits)**: angr Windows `python` fallback chain (Codex P2 #2 자기-모순 해소) → real subprocess `AuraCommandRunner` (POSIX pipe/fork/execvp/poll/drain + Windows CreatePipe/CreateProcess/reader threads) → `aura --probe-engines` CLI subcommand (envelope JSON, ADR option A) → `probe_engines_smoke` 통합 테스트 활성화 (WSL e2e GREEN) → pipe deadlock fix drain-while-waiting (Codex high #1) → 4 회귀 테스트 추가 + 2 production bug fix (poll cap + drain iteration cap, Codex medium #2). **Phase 2.5.2 (Slices X.8-X.8.2, 3 commits)**: probe binary lookup contract `aura_probe_resolve_binary` (env → vendored → PATH 우선순위, 5 probe 모두 통일, 5 env var 정의 — `AURA_RIZIN_BIN` / `AURA_GHIDRA_DECOMP_BIN` / `AURA_GHIDRA_FULL_ANALYZE_HEADLESS` / `AURA_JAVA_BIN` / `AURA_ANGR_PYTHON_BIN` / `AURA_RETDEC_DECOMPILER_BIN`) → CLI 가 `AURA_REPO_ROOT` seed (vendored 도달성, Codex review #4 high) + per-probe vendored regression (Codex review #4 medium) → empty `AURA_REPO_ROOT` edge case (Codex review #5 high). **Build infra (1 commit)**: MSVC `/FI` bare-name 으로 한글 경로 (`바탕 화면`) 빌드 차단 해소 — `aura.exe` Windows MSVC 빌드 GREEN, `aura --probe-engines` 직접 실행 GREEN, `probe_engines_smoke` Windows 도 GREEN (Codex review #6 high). **Codex final verdict: APPROVE**. **Test totals**: probe_unit 58 + subprocess_runner_unit 9 + consent_unit 15 + bootstrap_unit 17 = **99 cases GREEN**, ctest 5/5 suites GREEN (probe + subprocess + consent + bootstrap + probe_engines_smoke) on **양 OS (Windows MSVC 한글 경로 + Linux WSL Ubuntu 24.04)**. **Codex 5-round adversarial review cycle** (review #2~#6) — 각 round 별 모든 HIGH finding 닫힘. 미해결 = MEDIUM (CI 비-ASCII smoke step 부재) → Phase 9 platform coverage 백로그. **머지**: `claude/naughty-herschel-05ff4e` → main, `--no-ff` merge commit `65440808`, conflict 0. | (Phase 2.5 완전 종료) — 다음 후보 (a) Phase 2D ghidra-full opt-in vendoring + AuraDaemon.java, (b) Phase 7.1 angr `symbolic` request type 신설 + PRD §F-6 갱신, (c) Phase 11.5 SessionManager (concurrency primitives 첫 도입, §1.6 deferred 회수 + max_concurrent_sessions=3 통합), (d) install_hint 메시지 env var 안내 추가 (X.9, ~30줄). | 사용자 결정 대기: 다음 phase 우선순위. |
| 2026-05-05 | **🎉 Phase 2.5 Probe / Bootstrap / Consent — landed (Slices 1-60, merged to main)**. **Probe (slices 1-34)**: 5 engines (rizin/ghidra-decomp/ghidra-full/angr/retdec) AVAILABLE/RUNTIME_MISSING/ENGINE_MISSING/VERSION_MISMATCH/TIMEOUT 모든 분기 cover, install_hint OS-key split (Linux/macOS/Windows), raw_diagnostic stderr capture, in-memory TTL cache (5분, injectable clock), wire-stable status string + JSON envelope contract guards, minimum-version gates (rizin ≥ 0.7, angr ≥ 9.2). **Consent (slices 35-39, 53-55, 59-60)**: decision API (ASK_USER/GRANTED/DENIED/BLOCKED_FAILED), TRANSIENT cooldown gate, INTEGRITY/RESOURCE/DEPENDENCY no-retry blocked, SQLite store (`:memory:` test path), engine_consent.lock automation escape hatch + lock-aware wrapper. **Bootstrap (slices 40-58)**: 4-class error classification (network/sha256/disk/pip), retry-policy decision (1-shot TRANSIENT auto-retry / others ABORT), ScriptedFetch retry-loop dispatch, outcome → consent record write-back projection. **머지 단위**: 4 commits (probe / consent / bootstrap / roadmap-closure) + main merge (ADR-0048 흡수). 보류 자산 `tests/integration/cli_smoke/probe_engines_smoke.cpp` 는 본 머지 제외 (CLI subcommand 미구현 — CLI ADR 결정 후 활성화). **Test totals**: probe_unit 43 / consent_unit 15 / bootstrap_unit 17 = **75 cases / 297 assertions, ctest 3/3 unit suites GREEN** (probe + consent + bootstrap labels). **Deferred to Phase 11.5 SessionManager**: §1.6 per-engine mutex + global semaphore N=2 — 동일 primitive 가 SessionManager `max_concurrent_sessions=3` 와 자연 공유, 통합 설계 위해 보류. 현재 dispatch 는 single-threaded — production 단일 사용자 시나리오 충분, CI 자동화는 lock 파일로 우회. | (Phase 2.5 종료 → 다음 후보) (a) Phase 2D ghidra-full opt-in vendoring, (b) Phase 7.1 angr `symbolic` request type, (c) Phase 11.5 SessionManager (concurrency primitives 첫 도입, §1.6 deferred 회수), (d) CLI ADR 결정 (옵션 A: AURA_BUILD_CLI / B: aura-probe binary / C: defer). | 사용자 결정 대기: 다음 phase 우선순위. |
| 2026-05-04 | **Phase 11.5 + 11.6 closeout (bookkeeping)** — 사용자 직접 세션. ROADMAP 표 누락분 보강: (a) Phase 11.5 "P5 polish suite" 행 추가 (cycle-47 wrap-up 의 8 features 정리), (b) Phase 11.6 "Disasm flow arrow gutter (Cutter-style)" 행 추가. **ADR-0048 신규 작성** (D1 data path = 기존 pdj 재사용 / D2 lane 알고리즘 / D3 widget layered (sibling) / D4 hit-test reverse + 3-segment / D5 per-kind colours / D6 yForLineTest seam, R-9 위반 0 명시 + out-of-scope 4건 기록). Phase 11.6 코드는 land-time 이미 ctest 3/3 GREEN (T3 commit `8eb6eacd`); local closeout re-verify (build-rel `AURA_BUILD_GUI=OFF` default config) = orchestrator_smoke + cli_smoke 2/2 GREEN. 본 closeout 은 paper trail 만 — 코드 변경 0. 사용자 메모: **LLM 도입 방식 변경 결정** (자체 실행 ~1B 소형 모델이 중간 파이프라인 필터, 외부 LLM 은 MCP 또는 IPC gateway 로 접근, 직접 접근 X) — 본격 설계는 별도 긴 대화에서 진행. | (사용자 합의 대기) — 다음 phase 후보: (a) LLM pipeline 방향성 변경 design (별도 대화), (b) Phase 9 issue 08~10 정리, (c) PP1~4 v2 phase 분리, (d) PE backend block/edge 0 deep-dive. | 없음 |
| 2026-05-04 | **🎉 Cycle-47 (final) — 8h auto-session wrap-up**. **세션 종합**: 47 cycles, 13개 commits since session start (`c39f3683` → `f23cd3fd`). ctest -L integration: **3/3 GREEN, 0 RED** (전 세션 통틀어 마지막 baseline). **Stretch goal 달성**: 사용자 plan v2 의 RE 도구 차별화 4종 (PP4 자동 레이블링 + PP1 타입 전파 + PP2 구조체 필드 + PP3 배열/포인터) **ALL CLOSED v1**. ADR 4개 신규 (0044-0047), 새 CLI subcommand 4종 (label-candidates / propagate-type / field-candidates / array-candidates), 새 GUI dialog 4종 (Suggest-labels / Propagate-type / Suggest-fields / Suggest-array-shape), 신규 core module 1개 (type_propagation). **Display-only path = 5번 적용** (hex / PP4 / PP1 / PP2 / PP3) — pattern stable. **R-9 boundary 확장 정의**: "engine record + lookup/arithmetic OK / 추론 OR 분석 어휘 emit X" — Step 3 naming hygiene 가드 (cycle-37 회귀 test 로 박음). **장기 baseline 1건 해소** (cycle-38: cli_smoke override-on-empty). **P5 polish suite 8개 features 추가** (cycles 39-46): Recent Projects MRU / drag-drop binary / Go-to-address (Ctrl+G) / Find function (Ctrl+F) / Copy address-name / pane font zoom (Ctrl+= / Ctrl+- / Ctrl+0) / Keyboard shortcuts cheat sheet (Ctrl+/) / Dock focus (Ctrl+1/2/3/4). 모든 P5 feature 가 keyboardShortcutsHtml() 의 single source of truth 와 gui_smoke regression guard 로 보호됨. R-1 / R-9 / R-10 / R-12 위반 0 (전 cycle 통틀어). git push 0 (사용자 정책 준수). 본 cycle: ScheduleWakeup 생략 + Windows beep 1회 (8h 누적). 1 file +6 (ROADMAP only). | (세션 종료) | (계속) (1) PE backend block/edge 0 (Phase 2A backend gap, deep-dive 별 Phase). (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply (4 v2 phase 분리). |
| 2026-05-04 | **Cycle-46 landed — P5 polish: dock-focus shortcuts (Ctrl+1/2/3/4) (8h auto-session)**. plan v3 P5 여덟 번째. 4개 tabified workspace dock (Decompile / Disasm / CFG / Hex) 을 키보드로 바로 raise+focus. `src/gui/main_window.h`: 신규 public `bool focusDock(int n)` (n=1..4 → 각 dock; OOR → false). `src/gui/main_window.cpp`: focusDock = switch(n) → dock->raise() + show() + pane->setFocus(). View 메뉴 separator 후 4 actions ("Focus &Decompile" Ctrl+1, "Focus Di&sasm" Ctrl+2, "Focus &CFG" Ctrl+3, "Focus &Hex" Ctrl+4) — struct 배열 + lambda 로 간결하게. keyboardShortcutsHtml 에 4 행 추가 (cheat sheet 자동 갱신). gui_smoke 신규 SUBCASE: focusDock(1..4) all true, focusDock(0/-1/5) all false. 기존 keyboardShortcutsHtml SUBCASE 에 Ctrl+1..4 회귀 가드 추가. R-1/R-9/R-10 영향 0. ctest -L integration: 3/3 GREEN. 3 files +75/-1. **P5 polish suite 누적 = 8 features** (cycles 39-46): Recent Projects MRU, drag-drop binary, Go-to-address (Ctrl+G), Find function (Ctrl+F), Copy address/name, pane font zoom (Ctrl+= / Ctrl+- / Ctrl+0), Keyboard shortcuts cheat sheet (Ctrl+/), Dock focus (Ctrl+1/2/3/4). | cycle-47 (final 가능): 옵션 — (a) 마지막 작은 polish (예: Decompile pane 의 cursor-line addr indicator status bar 표시), (b) 세션 wrap-up summary commit + Windows beep (마지막 cycle protocol 따라 ScheduleWakeup 생략). 시간 예산 ~50min 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-45 landed — P5 polish: keyboard shortcuts cheat sheet (Ctrl+/) (8h auto-session)**. plan v3 P5 일곱 번째. 사용자가 cycles 39-44 에서 추가한 6 개 P5 단축키를 발견할 수 있도록 Help 메뉴에 "Keyboard shortcuts..." 다이얼로그 추가. **Single source of truth**: `static QString MainWindow::keyboardShortcutsHtml()` — Help 다이얼로그와 gui_smoke 가 같은 HTML 을 읽음. 추가 시 한 줄만 추가하면 다이얼로그와 회귀 가드 모두 자동 갱신. `src/gui/main_window.h`: 신규 static `keyboardShortcutsHtml()` + private slot `onShowKeyboardShortcuts()`. `src/gui/main_window.cpp`: HTML table format (`<tr><td>key</td><td>action</td></tr>`) — Ctrl+N/O/G/F/= /-/0// 모든 단축키 + Esc / Alt+Left/Right + drag-drop / Recent footnote. onShowKeyboardShortcuts = QMessageBox + RichText. Help menu 에 "&Keyboard shortcuts..." action + Ctrl+/ shortcut 추가 (separator 후 About). gui_smoke 신규 SUBCASE: keyboardShortcutsHtml() 호출 → cycle 39-45 에서 추가한 모든 단축키 (Ctrl+G / Ctrl+F / Ctrl+= / Ctrl+- / Ctrl+0 / Ctrl+/) + 기존 (Alt+Left / Alt+Right / Esc) 가 모두 포함됨 + 액션 라벨 키워드 ("Find function" / "Zoom" / "Recent" / "Drag") 검증. 회귀 가드: 미래에 단축키 제거 / 이름 변경 시 cheat sheet 갱신 안 하면 fail. R-1/R-9/R-10 영향 0. ctest -L integration: 3/3 GREEN. 3 files +85/-1. **P5 polish suite = 7 features 누적** (cycles 39-45). | cycle-46 (마지막일 가능성): 옵션 — (a) hex pane "Go to offset" Ctrl+G in hex (UI/UX 추가), (b) 더 큰 항목 — column sort 또는 status bar progress, (c) 세션 wrap-up + final summary commit. 시간 예산 ~1h 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-44 landed — P5 polish: pane font zoom (Ctrl+= / Ctrl+- / Ctrl+0) (8h auto-session)**. plan v3 P5 여섯 번째. 3개 pane (Decompile/Disasm/Hex) 의 monospace font size 를 단일 setter 로 동기 조정. **Per-pane changes**: 각 pane (`decompile_pane.{h,cpp}` / `disasm_pane.{h,cpp}` / `hex_pane.{h,cpp}`) 에 `setMonospaceFontPointSize(int)` (clamp [6, 32]) + `monospaceFontPointSize() const` 추가. QFont include 추가. **MainWindow**: kKeyPaneFontPt/kPaneFontDefaultPt(=10)/kPaneFontMin(=6)/kPaneFontMax(=32) constants. 신규 public `setPaneFontPointSize(int)` (3 panes 동시 호출 + clamp) + `paneFontPointSize() const` (Decompile pane canonical). 신규 slots `onZoomInPanes()` / `onZoomOutPanes()` / `onResetPaneZoom()` (status bar toast 포함). View 메뉴 separator 후 3 actions: "Zoom &In (panes)" QKeySequence::ZoomIn (Ctrl+=), "Zoom &Out (panes)" QKeySequence::ZoomOut (Ctrl+-), "&Reset pane zoom" Ctrl+0. restoreUiState 가 startup 에 setPaneFontPointSize(QSettings 값 또는 default) 호출, saveUiState 가 종료 시 paneFontPointSize() 저장. **gui_smoke 신규 SUBCASE**: openProject 후 setPaneFontPointSize(14) 호출 → paneFontPointSize() == 14, setPaneFontPointSize(0) → clamp 6, setPaneFontPointSize(999) → clamp 32, 마지막에 10 으로 reset 해 다른 SUBCASE 격리. R-1/R-9/R-10 영향 0. ctest -L integration: 3/3 GREEN. 7 files +120/-2. | cycle-45: 옵션 — (a) status bar progress (threading), (b) function table column sort enable (FunctionTableModel::sort 구현 + proxy mapping 신중), (c) 함수 정렬 단순 sort indicator (header click 만), (d) PE backend deep-dive. 시간 예산 ~1h 30min 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-43 landed — P5 polish: Copy address/name to clipboard (8h auto-session)**. plan v3 P5 다섯 번째. `src/gui/main_window.h`: 신규 public `QString clipboardTextForFunction(int row, QString kind) const` (test seam — gui_smoke 가 live clipboard 의존 없이 formatted text 검증, kind ∈ {"address", "name"}, OOB row 또는 unknown kind 면 empty). `src/gui/main_window.cpp`: clipboardTextForFunction 구현 (address → `"0x%1".arg(entry, 0, 16)`, name → m_functions[row].name verbatim, 그 외 empty). onFunctionContextMenu 의 dispatch chain 에 separator 후 "Copy address (0xXX)" / "Copy name (...)" 두 action 추가, 첫 dispatch branch (renameAct 분기 이전) 에서 QApplication::clipboard()->setText(clipboardTextForFunction(row, kind)) + status bar toast. QClipboard include 추가. gui_smoke 신규 SUBCASE: addBinary + analyze 후 row 0 의 address text shape 검증 ("0x" prefix + lowercase hex 만), name text non-empty, OOB (-1 / 99999) 와 unknown kind ("xyz") 모두 empty 반환. R-1/R-9/R-10 영향 0. ctest -L integration: 3/3 GREEN. 3 files +95/-1. | cycle-44: 옵션 — (a) function table 의 column sort enable (FunctionTableModel::sort 구현 필요), (b) status bar progress (threading), (c) recently-decompiled functions submenu, (d) decomp/disasm font-size shortcut (Ctrl+= / Ctrl+-). 시간 예산 ~1h 45min 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-42 landed — P5 polish: Find function by name (Ctrl+F) (8h auto-session)**. plan v3 P5 네 번째. `src/gui/main_window.h`: 신규 public `int findFunctionRowByName(QString needle) const` (case-insensitive substring scan over m_functions[].name; 빈 needle → -1, no-match → -1) + private slot `onFindFunction()`. `src/gui/main_window.cpp`: findFunctionRowByName 구현 (linear scan + Qt::CaseInsensitive contains), onFindFunction = QInputDialog → trim → findFunctionRowByName → hit 시 selectRow (cycle-41 의 jumpToFunction 와 같은 cascade), miss 시 status bar nudge. View 메뉴 Go-to-address 옆에 "&Find function..." action + QKeySequence::Find (Ctrl+F) shortcut 추가. gui_smoke 신규 SUBCASE: addBinary + analyze 후 첫 함수 name 의 2-char prefix 로 findFunctionRowByName 호출 → ≥0 hit + 빈 needle → -1 + 비현실적 needle (`"zzz_unlikely_token_xx7"`) → -1. R-1/R-9/R-10 영향 0. ctest -L integration: 3/3 GREEN (orchestrator + cli 21/21 + gui w/ 신규 find SUBCASE). 3 files +85/-1. | cycle-43: 옵션 — (a) Ctrl+Shift+G "find next" / F3 (cycle-42 의 자연 확장), (b) function table column sort 활성화 (이미 데이터 있음, sort header 만 enable), (c) 함수 우클릭 "Copy address/name to clipboard", (d) status bar progress (threading 필요), (e) PE backend deep-dive. 시간 예산 ~2h 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-41 landed — P5 polish: Go to address (Ctrl+G) (8h auto-session)**. plan v3 P5 세 번째. `src/gui/main_window.h`: 신규 public `bool jumpToFunction(quint64 addr)` (test seam — gui_smoke direct invoke 가능, dialog 없이 contract 검증) + private slot `onGoToAddress()`. `src/gui/main_window.cpp`: jumpToFunction = m_functions linear scan (helper rowOfFunctionByEntry 가 unnamed namespace 뒤에 있어 forward-reference 회피로 inline) → match 시 selectRow → onFunctionRowSelected cascade (decompile + disasm + cfg 자동 갱신), miss 시 status bar message + return false. onGoToAddress = QInputDialog::getText (placeholder "0x") → trim/strip "0x"/"0X" → toULongLong base 16 → jumpToFunction. View 메뉴 separator 후 "&Go to address..." action 추가, shortcut Ctrl+G. gui_smoke 신규 SUBCASE: openProject + addBinary + analyze 후 첫 함수 entry 추출 → jumpToFunction(entry) 가 true 반환 + 0xDEADBEEFCAFEBABE 호출이 false 반환. R-1/R-9/R-10 영향 0. ctest -L integration: 3/3 GREEN (orchestrator + cli 21/21 + gui w/ 신규 jumpToFunction SUBCASE). 3 files +85/-1. | cycle-42: 옵션 — (a) status bar progress, (b) theme switcher, (c) function table 의 name filter (QSortFilterProxyModel — 4-5 site mapping 필요), (d) hex→disasm reverse sync, (e) PE backend deep-dive. 시간 예산 ~2h 30min 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-40 landed — P5 polish: drag-drop binary onto window (8h auto-session)**. plan v3 P5 두 번째 항목. `src/gui/main_window.h`: 신규 public `int handleDroppedPaths(QStringList) `(test seam — gui_smoke 가 QDropEvent/QMimeData 합성 없이 contract 검증) + protected override `dragEnterEvent` / `dropEvent` 선언. `src/gui/main_window.cpp`: ctor 에 `setAcceptDrops(true)`, QDragEnterEvent / QDropEvent / QMimeData / QUrl includes 추가. dragEnterEvent = mimeData->hasUrls() 면 acceptProposedAction (real validation 은 dropEvent 에서). dropEvent = mimeData->urls() iterate, isLocalFile() 만 toLocalFile() 후 handleDroppedPaths 호출. handleDroppedPaths = m_projectDb null → status bar "open or create a project first" + return 0; 각 path 의 fileinfo isFile 체크 → addBinary() 호출 → added/rejected count 집계 → status bar message. gui_smoke 신규 SUBCASE: (1) 프로젝트 미오픈 상태에서 drop → 0 반환, (2) openProject + drop fixture → projectBinaryCount += 1, (3) non-existent path → 0 반환. R-1/R-9/R-10 영향 0 (UI event routing only, addBinary 재사용). ctest -L integration: 3/3 GREEN (orchestrator + cli 21/21 + gui w/ 신규 drag-drop SUBCASE). 3 files +95/-1. | cycle-41: 옵션 — (a) status bar progress (장시간 analyze 의 진행 상태 시각화 — QProgressBar + AURA_ANALYSIS_PROGRESS hook 필요), (b) theme switcher (light/dark palette toggle + QSettings persist), (c) F5 hex view → disasm reverse sync (ADR-0043 D5 deferred), (d) PE backend block/edge 0. 시간 예산 ~2h 45min 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **Cycle-39 landed — P5 polish: Recent Projects MRU (8h auto-session)**. plan v3 P5 (보조 GUI) 진입. `src/gui/main_window.h`: 신규 public `QStringList recentProjects() const` + private `pushRecentProject(path)` / `rebuildRecentMenu()` helper + `m_recentMenu` 멤버. `src/gui/main_window.cpp`: kKeyRecentProjects/kRecentMax (5) constants 추가, buildMenus 의 File 메뉴에 Open Project... 직후 "Open &Recent" 서브메뉴 삽입 (rebuildRecentMenu 호출), openProject 성공 직후 pushRecentProject 호출. pushRecentProject = QSettings 로드 + dedupe (removeAll) + prepend (newest first) + cap kRecentMax + save + rebuildRecentMenu. rebuildRecentMenu = clear + 빈 list 면 disabled placeholder, 아니면 numeric mnemonic (&1..&9) prefixed action 추가, 클릭 시 openProject(path). gui_smoke 신규 SUBCASE: QSettings reset → openProject(p1) → list size 1 / [p1] → openProject(p2) → [p2,p1] (newest first) → openProject(p1) again → [p1,p2] (dedupe + reorder no growth). cleanup 으로 다른 SUBCASE 격리. mid-cycle fix: gui_smoke.cpp 에 `#include <QSettings>` 추가 (선언 없음 컴파일 에러). ctest -L integration: 3/3 GREEN (orchestrator + cli 21/21 + gui w/ 신규 MRU SUBCASE). 3 files +110/-2. R-1/R-9/R-10 영향 0 (UI-only feature, 분석 코드 변경 0). | cycle-40: 옵션 — (a) drag-drop binary onto project view (QDragEnter/Drop on m_projectTable), (b) status bar progress + cancel button for analyze (QProgressBar + AURA_ANALYSIS_REQ_STATE polling), (c) PE backend block/edge 0 deep-dive. 시간 예산 ~3h 남음. | (계속) (1) PE backend block/edge 0. (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply. |
| 2026-05-04 | **🎉 Cycle-38 landed — cli_smoke override-on-empty baseline cleared → ctest -L integration 3/3 GREEN, ZERO RED (8h auto-session)**. Backlog 항목 (a) 처리. **Root cause**: `cli_smoke: override get on empty store returns not_found exit 1` 가 hardcoded `--func 0x40117b` 로 build_rich_key 를 거쳤는데, 해당 addr 가 PE fixture (Windows) 의 함수 entry 가 아니어서 (cross-fixture PE/ELF 레이아웃 차이) `analyze_for_get_failed` 가 먼저 반환됐음. **Fix**: TC 에 `--no-analyze` flag 추가 → skeleton key (fingerprint+addr) 로 직접 store_get 호출 → 빈 store → `not_found` (TC 의도와 일치). 1 file +13/-3, production code 변경 0. ctest -L integration: orchestrator + cli + gui = **3/3 GREEN** (이번 8시간 세션 처음으로 ALL GREEN). cli 21/21. PP4+PP1+PP2+PP3 4종 ALL CLOSED 직후 baseline RED 0 까지 도달. | cycle-39: 옵션 (b) PE backend block/edge 0 (Phase 2A backend gap) 또는 (c) plan v3 P5 보조 GUI (theme switcher / recent projects / drag-drop). 시간 예산 ~3h 15min 남음. | (계속) (1) PE backend block/edge 0 (이제 backlog 단독). (2) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply (4 v2 phases). |
| 2026-05-04 | **🎉 Phase 11.4.5 (P4.PP3) cycle-37 landed — naming-hygiene regression guard + PP3 closed → 4종 ALL CLOSED (8h auto-session)**. P4.PP3 C4 close-out per ADR-0047 D2. `src/gui/main_window.h`: `static QString MainWindow::formatArrayCandidateRow(const GuiArrayCandidate&)` 신규 — 다이얼로그 row 텍스트 합성을 단일 source-of-truth 로 추출, gui_smoke 가 직접 호출해 D2 가드를 회귀 검증할 수 있도록 노출. `src/gui/main_window.cpp`: formatArrayCandidateRow 구현 (3 source 전부 — op_str: base+offsets+diffs+gcd+sample / type_hint: var_id+type_str / var_cluster: offsets+diffs+gcd) + onFunctionContextMenu 의 dialog 루프 inlined-formatter 를 helper 호출로 단순화 (LOC -33). `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE: 3 synthetic GuiArrayCandidate (per source) → formatter 호출 → AURA-synthesized 부분 ("stride"/"array"/"element_type" 어휘 forbidden, case-insensitive) + type_hint 의 engine-verbatim typeStr 부분은 strip 후 검사 (engine 이 emit 한 "[" / English 단어 통과 OK). Positive shape (offsets=/diffs=/gcd= 토큰 존재) 도 검증해 formatter no-op 회귀까지 잡음. ctest -L integration: orchestrator + gui GREEN (6.26s, 신규 PP3 naming-hygiene SUBCASE 포함), cli 20/21 (1 RED 동일 pre-existing). 3 files +95/-44. **Phase 11.4.5 (P4.PP3) closed.** **🎯 사용자 plan v2 의 RE 도구 차별화 4종 (PP4 자동 레이블링 / PP1 타입 전파 / PP2 구조체 / PP3 배열) ALL CLOSED v1 — 8시간 자동 세션의 stretch goal 달성.** Display-only path = 5번 적용 (hex, PP4, PP1, PP2, PP3) — pattern stable. R-9 boundary = "engine record + lookup/arithmetic OK / 추론 OR 분석 어휘 emit X" 안정 정착. | cycle-38 (옵션): backlog 정리 — (a) cli_smoke override get-on-empty pre-existing baseline 분석 + 수정 시도 OR (b) PE backend block/edge 0 (Phase 2A backend 의 PE 경로) OR (c) plan v3 의 P5 (보조 GUI: theme switcher / recent projects / drag-drop). 시간 예산 ~3h 30min 남음. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply (4 v2 phases). |
| 2026-05-04 | **Phase 11.4.5 (P4.PP3) cycle-36 landed — GUI Suggest-array-shape popover + (a) op_str source GUI-only (8h auto-session)**. P4.PP3 C3 per ADR-0047 D1+D4. `src/gui/main_window.h`: GuiArrayCandidate struct (display-only per D5, source discriminator: "var_cluster"|"op_str"|"type_hint", arithmetic-only fields offsets/diffs/gcd + engine-verbatim typeStr) + generateArrayCandidates(funcAddr) public 메서드. `src/gui/main_window.cpp`: gcd_i64_abs helper + scanBracketOffsets helper (CLI match_bracket_offset 의 QString port — `[<reg>+0xN]` 패턴 lightweight 스캐너), generateArrayCandidates 구현 — (a) op_str: disasmList(funcAddr) iterate + scanBracketOffsets per instruction + group by base_reg (≥2 offsets gate) + sort + diffs + gcd, (b) type_hint: m_typeFacts filter by function_id AND typeStr.contains('['), (c) var_cluster: stack vars (≥2) sorted by stackOffset + diffs + gcd — single combined entry. onFunctionContextMenu 에 8번째 entry "Suggest &array shape..." 추가, runDisasm(fa) lazy-fill 후 generateArrayCandidates 호출, 신규 QDialog (PP2 dialog 패턴 답습) — naming-hygiene compliant hint label ("raw arithmetic only — 자료형 정의 합성은 R-9 위반으로 사용자/LLM 영구 위임"), QListWidget per-source row text formatter (op_str: base+offsets+diffs+gcd+sample_addr / type_hint: var_id+type_str / var_cluster: offsets+diffs+gcd, **모두 arithmetic 어휘만**), Apply (statusBar count toast)/Cancel. **D2 naming-hygiene 가드 명시 in source comments**: dialog text 합성 코드에 "stride"/"array"/"element_type" 단어 emit 없음 (오직 "offsets"/"diffs"/"gcd"/"sample"/"base"/"var_id"/"type_str"). `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE: function walk + runDisasm + generateArrayCandidates + per-source contract 검증 (op_str/var_cluster: offsets sorted, diffs.size==offsets.size-1; type_hint: var_id≠0, typeStr contains '['). ctest -L integration: orchestrator + gui GREEN (6.39s, 신규 PP3 contract TC 포함), cli 20/21 (1 RED 동일 pre-existing). 3 files +245/-1. | cycle-37 (P4.PP3 C4 close — PP3 종료, **plan v2 차별화 4종 ALL CLOSED**): GUI naming-hygiene 회귀 가드 추가 (gui_smoke text 합성 출력에 forbidden token 검사) + D5 contract 강화 + ROADMAP close-out. PP4+PP1+PP2+PP3 = **사용자 plan v2 의 RE 도구 차별화 4종 모두 v1 closed** 달성. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4 LLM v2 + PP1 v2 store wire + PP2 (a) op_str v2 + PP3 v2 apply (4 v2 phases deferred). |
| 2026-05-04 | **Phase 11.4.5 (P4.PP3) cycle-35 landed — CLI array-candidates + heuristic (b)+(c) (8h auto-session)**. P4.PP3 C2 per ADR-0047 D1+D3. `src/cli/main.cpp`: cmd_arrc subcommand (`--func <hex>`) + run_array_candidates 함수 — analyze 호출 → function_id resolve → (c) stack variables sorted by stack_offset → emit `variable_clusters: [{var_ids, offsets, diffs, gcd}]` (raw arithmetic only, **NO** "stride"/"array"/"element_type" 합성), (b) opportunistic — type_facts where function_id==target AND type_str contains '[' → emit `type_hints: [{var_id, type_str}]` (engine literal verbatim). `candidates: []` (a) op_str source v1 비활성 (DISASM body 분리 — PP2 cycle-32 와 같은 사유). gcd_i32 helper 신규 (Euclidean over signed 32-bit). `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TDD TC — analyze 첫 entry → array-candidates 호출 → JSON shape 검증 (command, function_addr, candidates/variable_clusters/type_hints arrays + entry shapes when non-empty) + **R-9/D2 naming-hygiene regression guard**: `r.stdout_.find("stride") == npos`, `find("element_type") == npos`, `find("\"array\"") == npos` (engine literal "[" / "*" 통과, AURA 분석 어휘 차단). ctest -L integration: orchestrator + gui GREEN, cli 20/21 (신규 array-candidates TC PASS, 1 RED 동일 pre-existing). 2 files +254. | cycle-36 (P4.PP3 C3): GUI "Suggest array shape..." popover + (a) op_str source 활성화 (`disasmList(funcAddr)` in-process stride grouping, GUI-only) + Variable/Function 우클릭 menu + gui_smoke contract TC. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4/PP1/PP2 v2 wires + PP3 v2 apply. |
| 2026-05-04 | **Phase 11.4.5 (P4.PP3) cycle-34 landed — design grilling + ADR-0047 (8h auto-session)**. P4.PP3 entry — plan v2 차별화 4종 중 마지막 (배열/포인터 표현: stride raw stat 후보 + 사용자/LLM 선택). Plan sub-agent (multi-agent token-saving, 165s, 27 tool) 으로 5 design 결정 grilling. **핵심 통찰들**: (1) **Display-only path 5번째 채택** — pattern 정착 단계. (2) **D2 새 차원 추가 — "Step 3 naming hygiene" 가드**: arithmetic emit OK (count/diff/gcd 는 분석 X), **분석 어휘 ("stride"/"array"/"element_type") emit 영구 거부** — PP1~PP2 묵시적 가드를 PP3 에서 명시 필수 (사용자 directive "array 후보" 가 자연어 hint 욕구로 흐를 위험). (3) PP2 의 deferred (a) op_str source 가 PP3 GUI-only path 에서 자연 해결 — `MainWindow::disasmList(funcAddr)` 가 이미 in-process cache (cycle-19/Phase 11.3.10), GUI 만 stride grouping in-process. CLI 는 PP2 와 같은 이유로 deferred. (4) D5 v1 = no-apply (statusBar count) — R-5 위반 회피. apply path 별도 enum 필요 → v2 별 ADR. cycle 분해 4 cycle (ADR / CLI / GUI w/ op_str source / contract+close). 2 files +200 (ADR + ROADMAP, 코드 변경 0). | cycle-35 (P4.PP3 C2): CLI `aura array-candidates --func <hex>` + heuristic (b) opportunistic type_str + (c) variable cluster + cli_smoke TC. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4/PP1/PP2 v2 wires + PP3 v2 apply. |
| 2026-05-04 | **🎉 Phase 11.4.4 (P4.PP2) cycle-33 landed — GUI Suggest-fields + PP2 closed (8h auto-session)**. P4.PP2 C3 (+ ROADMAP close — C4 통합) per ADR-0046 D4. `src/gui/main_window.h`: GuiFieldCandidate struct (display-only per D5) + generateFieldCandidates(funcAddr) public 메서드. `src/gui/main_window.cpp`: helper 구현 (m_variables filter by function_id + kind=="stack" + sort by stackOffset), onFunctionContextMenu 에 "Suggest fields..." 7번째 entry 추가, QDialog 신규 — "stack-offset cluster (v1). op_str regex 후보는 v2 — DISASM body 통합 후 활성. struct 정의 합성은 R-9 위반으로 사용자/LLM 영구 위임" hint label + QListWidget (per-row checkbox) + Apply (count statusBar)/Cancel. PP1 dialog 패턴 직접 답습 (consistency). `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE: 함수 walk + generateFieldCandidates 호출 → contract 검증 (varId ≠ 0, kind=="stack", stackOffset 정렬 검증). ctest -L integration: orchestrator + gui GREEN (6.27s, 신규 PP2 contract TC 포함), cli 19/20 (1 RED 동일 pre-existing). 3 files +145/-2. **Phase 11.4.4 (P4.PP2) closed**. **PP4 + PP1 + PP2 = 사용자 plan v2 차별화 4종 중 3종 완료**. | cycle-34 (P4.PP3 entry — 마지막): PP3 (배열·포인터 표현) design grilling via Plan sub-agent → ADR-0047. plan v2 의 분해: hover/click 시 표현 전환 UI (ptr ↔ array). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP2 (a) op_str source v2. |
| 2026-05-04 | **Phase 11.4.4 (P4.PP2) cycle-32 landed — CLI field-candidates + stack cluster heuristic (8h auto-session)**. P4.PP2 C2 per ADR-0046 D1+D3. `src/cli/main.cpp`: cmd_field_candidates subcommand (`--func <hex>`) + run_field_candidates 함수 — analyze 호출 → function_id resolve → (c) stack_offset cluster (variables where function_id matches and kind=="stack", sorted by stack_offset) → JSON `{command, function_addr, candidates: [], stack_clusters: [{var_id, stack_offset, name, kind: "stack"}]}` emit. **(a) op_str regex source DEFERRED to PP2 v2** — analyze body 가 ADR-0040 D4 의 body 분리 정합 으로 instructions[] 미보유 (DisasmBody 로 분리), v1 에 (a) 활성화 = DISASM dispatch 별도 + per-function 호출 추가 wiring 필요 → 별도 cycle. ADR-0046 본문에서도 v1 = op_str source 없이 stack cluster 만 명시. **mid-cycle fix**: 첫 빌드에서 `instructions_count` C2039 — analyze body 의 멤버가 아님을 빠르게 캐치하여 (a) source 코드 블록 제거 (R-9 가드 정합 — 분석 안 하는 게 정합). `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TDD TC: analyze entry 발견 → field-candidates 호출 → JSON shape 검증 (command + function_addr + candidates array + stack_clusters array, 둘 다 0 도 graceful, 있으면 entry shape 검증). ctest -L integration: orchestrator + gui GREEN, cli 19/20 (신규 field-candidates TC PASS, 1 RED 동일 pre-existing). 2 files +210/-1. | cycle-33 (P4.PP2 C3): GUI "Suggest fields..." popover (PP1 의 Propagate-type dialog 패턴 답습) + Variable/Function 우클릭 menu + gui_smoke contract TC. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP2 (a) op_str source v2. |
| 2026-05-04 | **Phase 11.4.4 (P4.PP2) cycle-31 landed — design grilling + ADR-0046 (8h auto-session)**. P4.PP2 entry — 사용자 plan v2 차별화 4종 중 3번째 (구조체 필드 오인식: offset cluster 후보 + 사용자/LLM 선택). Plan sub-agent (multi-agent token-saving, 122s, 25 tool) 으로 5 design 결정 grilling. **핵심 통찰들**: (1) **D2 R-9 boundary 정밀화** — op_str regex 매칭 = "single record field substring 추출" = lookup OK (PP4 의 string.content → snake_case 와 동형), **단 "offset cluster → struct 정의 합성" 영구 거부** (R-9 직격탄). (2) **Display-only path 4번째 채택** — hex/PP4/PP1/PP2 — pattern stable. ADR 본문에 4-step decision tree 명문화 (소스=record field? 조작=select+group+count? 합성=새 분석객체 X? Apply=기존 slot?). PP3 도 같은 tree 적용 가능 명시. (3) D5 PP1 의 `AURA_OVERRIDE_PAYLOAD_TYPE` 슬롯 재활용 + PP1 v2 store wire deferral 상속. struct definition 자체 저장 슬롯은 v3 별 ADR (`AURA_OVERRIDE_PAYLOAD_STRUCT_DEF` 신규 enum 필요, 본 plan 외). cycle 분해 4 cycle (ADR / CLI / GUI / contract). 신규 module `src/core/field_candidates.{h,cpp}` 옵션 (PP1 type_propagation 답습). 2 files +180 (ADR + ROADMAP, 코드 변경 0). | cycle-32 (P4.PP2 C2): CLI `aura field-candidates --func <hex> [--var]` + heuristic (a) op_str regex parse `[<reg>+0xNN]` + (c) stack_offset cluster + ranking + cli_smoke TC. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4/PP1 v2 wires deferred. |
| 2026-05-04 | **🎉 Phase 11.4.3 (P4.PP1) cycle-30 landed — Apply 활성화 (status bar count) + PP1 closed (8h auto-session)**. P4.PP1 C5 close-out per ADR-0045. main_window.cpp 의 propAct 분기에서 applyBtn->setEnabled(true), connect Apply → dlg.accept, Accepted 시 list 의 ItemIsUserCheckable + Qt::Checked row count → statusBar message 출력 (`"Propagate type (dry-run): N개 candidate 선택됨 [target=...] — store 통합은 PP1 v2 phase 에서"`). **Variable-target store_put 통합은 별도 phase 분리** — buildKeyForVariable() 신설 필요 (현재 buildKeyForFunction 만 존재, override_identity 의 variable target path 가 아직 GUI 에 wired 안 됨). R-5 영향 0 (store touch 0). gui_smoke 신규 SUBCASE: function table walk + generateTypePropagationCandidates 호출 → 후보 contract 검증 (varId/functionId ≠ 0, distance==1, source=="callee_param", newType=="UserStruct*", selected=true 기본값). ctest -L integration: orchestrator + gui GREEN (6.23s, 신규 PP1 contract TC 포함), cli 18/19 (1 RED 동일 pre-existing). 2 files +60/-3. **Phase 11.4.3 (P4.PP1) closed**. PP4 + PP1 = 사용자 plan v2 의 RE 도구 차별화 4종 중 2종 완료. | cycle-31 (P4 entry — PP2): PP2 (구조체 필드 오인식 — offset cluster + LLM 의미 추론) design grilling via Plan sub-agent → ADR-0046. plan v2 의 분해: 자동 후보 생성 + 사람 직접 선택 or LLM 자동 선택. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4 LLM v2 deferred. (4) PP1 v2 store wire deferred. |
| 2026-05-04 | **Phase 11.4.3 (P4.PP1) cycle-29 landed — GUI Propagate-type popover (Apply stub) (8h auto-session)**. P4.PP1 C4 per ADR-0045 D5. `src/gui/main_window.h`: 신규 GuiCallEdgeRecord/GuiVariableRecord/GuiTypeFactRecord struct (Phase 11.3.5/11.3.6 mirror 패턴 답습) + GuiTypePropagationCandidate struct (display-only per D5, NOT in AuraRecordCollection) + generateTypePropagationCandidates(funcAddr, targetType) public 메서드 + 3 멤버 (m_callEdges/m_variables/m_typeFacts) 추가. `src/gui/main_window.cpp`: analyze post-step 에서 3 record mirror 추가 (call_edges/variables/type_facts), generateTypePropagationCandidates 구현 (CLI 알고리즘과 1:1 — caller_id 매칭 + arg index by function_id + type_fact lookup by var_id, R-9 boundary 동일), onFunctionContextMenu 에 "Propagate type..." entry 추가. QDialog 신규: target-type LineEdit (live update — textChanged → refresh) + QListWidget (per-row checkbox, ItemIsUserCheckable + UserRole data: var_id/function_id/newType) + Apply (cycle-30 wired, 현재 disabled + tooltip)/Cancel. 빈 결과 케이스도 graceful (placeholder item). QLabel/QLineEdit include 추가. ctest -L integration: orchestrator + gui GREEN (6.26s, 회귀 0), cli 18/19 (1 RED 동일 pre-existing). 2 files +210/-2. | cycle-30 (P4.PP1 C5 close — PP1 종료): Apply path 통합 — selected candidates → buildKeyForVariable (또는 신규 ID 생성) + override_store_put(AURA_OVERRIDE_PAYLOAD_TYPE) 일괄 + statusBar toast + QDialog applyBtn enabled + gui_smoke TC. PP1 closed → PP2 (struct field) 또는 PP3 (array 표현) 진입. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.4.3 (P4.PP1) cycle-28 landed — CLI propagate-type (dry-run) (8h auto-session)**. P4.PP1 C3 per ADR-0045 D4. `src/cli/main.cpp`: cmd_propagate_type subcommand (`--func <hex>` + `--target-type "<T>"` + `--apply` flag) + run_propagate_type 함수 — analyze 호출 → aura_type_propagation_generate → JSON {command, function_addr, target_type, dry_run=true, candidates: [{var_id, function_id, call_site_addr, old_type, new_type, distance, source: "callee_param"}]} emit. `--apply` 는 v2 (cycle-30 with --candidate-ids 부분 선택) — 현재 호출 시 not_implemented_v1 에러 반환 (R-5 정합 — 사용자 의도 명시 없이 store 쓰기 거부). `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TDD TC: analyze 첫 함수 entry → propagate-type --target-type "UserStruct*" 호출 → JSON shape 검증 (command + function_addr + target_type + dry_run=true + candidates array — 첫 entry 의 callee args 결과는 fixture-dependent, 0 도 graceful, 있으면 source="callee_param" + new_type="UserStruct*" 검증). ctest -L integration: orchestrator + gui GREEN, cli 18/19 (신규 propagate-type TC PASS, 1 RED 동일 pre-existing). 2 files +145/-1. | cycle-29 (P4.PP1 C4): GUI tree dialog — Variable / Function 우클릭 → Propagation Tree QDialog (QTreeView + checkbox + target-type LineEdit) + Apply (cycle-30 에서 wire). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.4.3 (P4.PP1) cycle-27 landed — type_propagation derived view algorithm module (8h auto-session)**. P4.PP1 C2 per ADR-0045 D2. 신규 `include/aura/type_propagation.h` (extern "C" 인터페이스 — `AuraTypePropagationCandidate` struct + `aura_type_propagation_generate(body, caller_entry, target_type, *out)` + result_free) — display-only path 명시 (R-9 boundary 헤더 주석에 박음). 신규 `src/core/type_propagation.cpp` (derived view 구현): (1) caller function_id resolve by entry, (2) variables 인덱스 by function_id 의 "arg" kind only, (3) type_facts 인덱스 by var_id, (4) call_edges where caller_id 매칭 + callee_id ≠ NULL → callee args enumerate → 각 arg 마다 candidate (var_id, function_id=callee, call_site_addr, old_type from typeFact lookup, new_type=target_type, distance=1, source="callee_param") emit, (5) std::vector raw → malloc 블록으로 변환해 C 호출자도 사용 가능. CMakeLists `AURA_CORE_SOURCES` 에 추가. **Display-only contract 정합 검증**: AuraRecordCollection 추가 0, body schema bump 0, AuraEngineResponse mutate 0 — module 의 모든 변경은 lookup-only. **R-9 명시**: indirect call (callee_id == AURA_NULL_ID) 는 후보 미생성 (false positive 회피 > 완전성). 3 files +175 (header + impl + CMakeLists). ctest -L integration: orchestrator + gui GREEN, cli 17/18 (1 RED 동일 pre-existing). | cycle-28 (P4.PP1 C3): CLI `aura propagate-type --func <hex> --target-type "<T>" --dry-run` (default dry-run) — `aura_type_propagation_generate` 호출 + JSON candidates emit + cli_smoke TC. (note: ADR-0045 D4 의 `--var <var_id>` 대신 `--func <hex>` 로 시작 — caller function 단위 grouping, 더 단순.) | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.4.3 (P4.PP1) cycle-26 landed — design grilling + ADR-0045 (8h auto-session)**. P4.PP1 entry — 사용자 plan v2 의 RE 도구 차별화 4종 중 가장 강조 (수천 변수 수동 타입 수정 노가다 제거 — 트리 + 체크박스 부분 선택). Plan sub-agent (multi-agent token-saving, 111s, 11 tool) 으로 5 design 결정 grilling. **핵심 통찰 — display-only path 3번째 채택**: ADR-0043 hex pane → ADR-0044 PP4 → ADR-0045 PP1 — 동일 패턴 (1급 record 거부, AuraRecordCollection 추가 0, body schema bump 0, derived view) 으로 R-9 boundary 가 "engine record + lookup OK / 추론 X" 로 안정 정착. D2 = caller var → callee parameter **1-step only** (alias chain 영구 거부 — R-9 직격탄, return type 양방향 v2 deferred — engine return-variable 누락으로 false positive). D2 의 진짜 R-9 회피: caller 측 calling-convention 해석조차 거부 → engine 이 caller arg 매핑 emit 안 하면 후보 미생성 (false positive 회피 > 완전성). D3 = `AURA_OVERRIDE_PAYLOAD_TYPE = 1` 기존 enum 슬롯 재활용 (override_store.h:50, 사용 0 → schema bump 0, R-5 정합 자동). D4 = `aura propagate-type --var --target-type --dry-run` default + `--apply --candidate-ids` 부분 선택. D5 = Variable 우클릭 → Propagation Tree QDialog (QTreeView + checkbox + target-type LineEdit + Apply/Cancel) — PP4 popover 패턴 답습. 5 cycle 분해 + C6 옵션 (mmap 캐시 — candidate set > 100k binary 한정). 2 files +180 (ADR + ROADMAP, 코드 변경 0). | cycle-27 (P4.PP1 C2): `src/core/type_propagation.{h,cpp}` 신규 — derived view 알고리즘 (call_edge × callee args + typeFact lookup → candidate vector) + unit test (3-function chain fixture). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. (3) PP4 LLM v2 deferred. |
| 2026-05-04 | **Phase 11.4.2 (P4.PP4) cycle-25 landed — GUI popover (Suggest labels...) (8h auto-session)**. P4.PP4 C3 per ADR-0044 D3. `src/gui/main_window.h`: GuiLabelCandidate struct (display-only per D5 — NOT in AuraRecordCollection, NOT 1급 record) + generateLabelCandidates(funcAddr) public 메서드. `src/gui/main_window.cpp`: deriveNameFromSymbol/snakeCaseFromString helpers (CLI 와 동일 로직 in-process 포팅 — m_xrefs/m_symbols/m_strings 직접 사용, RPC/dispatch 없음 = O(n) 스캔만), generateLabelCandidates 구현 (xref 가 function range 안 + to_addr 가 import/string 매칭 → candidate, sort+dedupe+top-5). onFunctionContextMenu 에 "Suggest labels..." 6번째 entry 추가 (separator 후) → 사용자 선택 시 generateLabelCandidates 호출 → 후보 0 이면 QMessageBox.information, 후보 있으면 modal QDialog (QListWidget + Apply/Cancel) → Apply 시 renameFunctionAt(row, name) 재사용 (R-5 정합 — override_store_put RENAME). QDialog/QListWidget include 추가. `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE: function table model 에서 entry 파싱 후 generateLabelCandidates 호출, 각 함수의 결과 contract 검증 (≤5, name non-empty, source ∈ {import,string}, confidence ∈ (0,1]). 후보 0 도 graceful (sawAny != requirement). ctest -L integration: orchestrator + gui GREEN (6.24s, 신규 PP4 contract TC 포함), cli 17/18 (1 RED 동일 pre-existing). 3 files +200/-2. | cycle-26 (P4.PP4 C4 close — F4 종료): LLM source 추가 (D4 v2) — GUI-only LlmClient 호출, timeout 5s, fallback (LLM 실패 시 heuristic 후보 그대로). 또는 PP4 close + PP1 (type propagation) 진입. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.4.2 (P4.PP4) cycle-24 landed — CLI label-candidates + heuristic (8h auto-session)**. P4.PP4 C2 per ADR-0044. `src/cli/main.cpp`: cmd_label_candidates subcommand + `--func <hex>` 인자 + run_label_candidates 함수 (analyze 호출 → body 의 functions/xrefs/symbols/strings → heuristic algorithm → JSON emit). Heuristic 구현 ADR-0044 sketch 그대로: (a) xref `from_addr ∈ [func.entry, func.entry+size)` + `to_addr` 매칭 IMPORT symbol 또는 string → candidate 생성, (b) snake_case_from_string/derive_name_from_symbol helper (lowercase + non-alnum→_, "imp.printf"→"printf"), (c) std::sort by confidence desc + source preference (string > import), dedupe by name, top-5. **Display-only path 정합 검증**: AuraRecordCollection 추가 0, body schema bump 0, AuraEngineResponse mutate 0 — cycle 의 모든 변경은 cli/main.cpp 의 새 transient JSON path 만. `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TDD TC: analyze entry 발견 → label-candidates 호출 → JSON shape 검증 (command + function_addr + candidates array ≤5 + 각 entry shape: name/source/confidence/evidence + source ∈ {string, import}). PE/ELF fixture 첫 함수의 candidates 가 0 일 수 있어 (xref 가 그 함수 시작 영역 밖) array length 0 도 허용. ctest -L integration: orchestrator + gui GREEN, cli_smoke 17/18 (신규 label-candidates TC PASS, 1 RED 동일 pre-existing override). 2 files +200/-1. | cycle-25 (P4.PP4 C3): GUI popover — DecompilePane context menu 6번째 entry "Suggest labels..." + main_window 의 QDialog (≤5 candidates list + Apply 버튼) + Apply path = 기존 renameFunctionAt 재사용 + gui_smoke. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.4.2 (P4.PP4) cycle-23 landed — design grilling + ADR-0044 (8h auto-session)**. P4 entry — 사용자 plan v2 의 RE 도구 차별화 4종 중 첫 (PP4 자동 레이블링: LAB_xxxxx → 의미명 후보 + 사용자 선택). Plan sub-agent (multi-agent token-saving, 132s, 25 tool) 으로 5 design 결정 grilling. **핵심 통찰 (D5)**: LabelCandidate = **display-only path** (1급 record 거부, body schema bump X) → ADR-0043 hex pane 패턴 정확한 재활용 → R-9 자연 우회 (record 화하면 "AURA 가 분석 추론" 위반). D1 = heuristic (a) string ref + (b) call pattern (import symbol) v1, (d) LLM v2 (회귀 가시성 보호). D2 = 별도 `aura label-candidates --func <hex>` CLI (llm-context 와 SRP 분리). D3 = 함수 우클릭 popover only (별도 dock 거부). D4 = LLM 은 GUI-only (ADR-0033 "AURA core 무 LLM" 보존). 선결 자산 모두 wired (xrefs/symbols/strings 가 Phase 11.3.5/11.3.6 에서 이미 record 화됨). Heuristic ranking 알고리즘 ADR 본문에 sketch (xref `from_addr ∈ function range` + `to_addr` matches symbol/string + confidence-sorted top-5). Apply path = 기존 `override_store_put RENAME` (cycle-9/16 검증) — R-5 정합. 2 files +160 (ADR + ROADMAP, 코드 변경 0). | cycle-24 (P4.PP4 C2): CLI `aura label-candidates --func <hex>` + heuristic (a)+(b) 구현 — `src/cli/main.cpp` 에 cmd_label_candidates + run_label_candidates (xref iteration + symbol/string match + ranking + JSON emit) + cli_smoke TC. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.4.1 (P3 packaging) cycle-22 landed — cmake --install 검증 (8h auto-session)**. Plan v3 의 P3 (패키징) 진입. `cmake --install build-rel --config Debug --prefix build-rel/install-test` 으로 isolated install 실행 → 결과 검증: (a) `bin/aura.exe` 4.x MB + `bin/aura-gui.exe` 5.x MB 배포 (CLI subcommand 패키지 + Qt6 GUI), (b) `include/aura/*.h` 전체 public 헤더 (engine_request/orchestrator/normalized_records/rizin_adapter/override_*/mapped_file/...), (c) `share/doc/aura/README.md + LICENSE`. installed `aura.exe --version` 호출 → "aura 0.1.0" GREEN. CMakeLists 수정 0 (기존 install rules 가 정확히 동작 — Phase 11.3 의 install(TARGETS) 와 install(DIRECTORY include) 가 미리 깔려 있어 추가 작업 불필요). 1 file +1/-1 (ROADMAP only). | cycle-23 (P4 entry — 사용자 plan v2 강조 차별화 영역): PP4 (자동 레이블링) design grilling via Plan sub-agent → ADR-0044. PP4 가 가장 즉시 효과 큼 + 데이터 의존성 가장 단순 (xrefs[]/symbols[]/strings[] 이미 cycle 1~7 에서 wired). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **🎉 Phase 11.3.10 (P2.F5) cycle-21 landed — cross-pane sync; F5 closed; P2 ALL CLOSED (8h auto-session)**. P2.F5 C4 close-out per ADR-0043 D5. main_window.cpp 에 connect 2줄 추가: `disasmPane.instructionSelected → hexPane.scrollToAddr`, `cfgPane.blockActivated → hexPane.scrollToAddr`. F4 mediator 패턴 답습 — main_window 단일 mediator 위에 connect line 만 추가. Reverse (hex click → disasm) 는 ADR-0043 D5 명시 deferred (nearest-instruction snap edge case). gui_smoke 신규 SUBCASE: 함수 disassembleFunctionAt(0) → 첫 instruction 의 hex addr 파싱 → disasm.selectInstruction(addr) → 그 결과 hex.windowStartOffset/Bytes 가 addr 를 cover (`addr >= ws && addr < ws+wb`) 검증. ctest -L integration: orchestrator + gui GREEN (6.22s, 신규 sync TC 포함), cli 16/17 (1 RED 동일 pre-existing). **Phase 11.3.10 (P2.F5) closed**. **Phase 11.3 series GUI 기초 4 sub-feature 모두 closed**: F2 Disasm view (11.3.7) + F3 CFG view (11.3.8) + F4 Decomp 강화 (11.3.9) + F5 Hex view (11.3.10). 사용자 plan v3 의 P2 (기초 RE 기능 — Ghidra/Cutter/IDA Pro 레벨 기초) **전체 종료**. 2 files +60/-1. | cycle-22 (P3 또는 P4 분기 — 사용자 plan v3 의 우선순위): 옵션 A = P3 패키징 (cmake --install 검증 + Phase 11.4.1), 옵션 B = P4 페인포인트 진입 (PP4 자동 레이블링 → PP1 타입 전파 → PP2 구조체 → PP3 배열). plan v3 의 P3 가 P4 보다 우선 표시되어 있으나 PP 작업이 사용자가 plan v2 round 에서 가장 강조한 차별화 영역. 다음 wake 에서 결정. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.10 (P2.F5) cycle-20 landed — window paging via scrollToAddr (8h auto-session)**. P2.F5 C3 per ADR-0043 D4. HexPane 가 fixed-size window (32 KB = 2048 rows) 만 QPlainTextEdit 에 보유 — 100MB+ binary 도 즉시 응답 보장. 신규 멤버 `m_windowStart`, `m_windowBytes` (current rendered window byte offsets). `fillRange(start, byteCount)` 메서드 — start 를 row boundary 로 align down, m_mf->size 로 clamp, formatRow 로 행 단위 렌더 + setPlainText. `renderInitialWindow` 제거하고 `fillRange(0, kWindowBytes)` 로 통합. `scrollToAddr(addr)`: addr 가 current window 밖이면 fillRange(addr - kWindowBytes/2, kWindowBytes) 로 re-page (작은 file 인 경우 newStart=0 으로 자연 clamp), window 안이면 cursor 만 이동 (flicker 방지). 새 test hooks `windowStartOffset()`, `windowByteCount()`. gui_smoke 신규 SUBCASE: scrollToAddr(near_end) 호출 → window 가 addr 를 항상 cover (`addr >= ws && addr < ws+wb`) + window start 가 row boundary (`ws % 16 == 0`) 검증. ctest -L integration: orchestrator + gui GREEN (6.30s), cli 16/17 (1 RED 동일 pre-existing). 3 files +75/-25. | cycle-21 (P2.F5 C4 close — F5 종료, P2 종료): cross-pane sync in (ADR-0043 D5) — main_window connect 2줄: disasm.instructionSelected → hex.scrollToAddr, cfg.blockActivated → hex.scrollToAddr. Phase 11.3.10 closed → **P2 (기초 RE 기능) 전체 종료**. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.10 (P2.F5) cycle-19 landed — HexPane + MappedFile + 기본 렌더 (8h auto-session)**. P2.F5 C2 per ADR-0043 D1+D3. 신규 `src/gui/hex_pane.{h,cpp}` (QPlainTextEdit monospace, MappedFile* 직접 보유 — display-only path per R-12 single-source-of-truth, AuraEngineResponse 거치지 않음). 행 format `<addr16hex>  <16 byte XX XX>  <ASCII non-printable=.>`, formatRow helper, openBinary/closeBinary lifecycle (mapped_file_open/destroy), C2 단계 = 첫 16KB 까지 일괄 렌더 (~1024 행), viewport-driven lazy 는 cycle-20 (C3). scrollToAddr slot 미리 추가 (file_offset==addr v1 가정, VA→file 변환은 D1 v2 deferred). `src/gui/main_window.{h,cpp}`: HexPane forward decl + m_hexDock/m_hexPane + 4번째 tabified dock (decompile/disasm/cfg/hex 그룹), analyze post-step 에서 m_hexPane->openBinary(currentBinaryPath) 자동 호출, hex_pane.h include. CMakeLists 양쪽 (gui + gui_smoke) 신규 source 추가. `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE: hexDock objectName + qobject_cast<HexPane> + fileSize > 0 + lineCount ≥ 1 + 첫 행 검증 (offset=0000000000000000 + ELF magic "7f 45 4c 46" 또는 PE magic "4d 5a"). **R-9/R-12 가드 명시**: hex bytes 가 InstructionRecord.bytes 와 동일 데이터일 수 있으나 hex pane 은 record/RPC 거치지 않고 직접 mmap read — display-only path, normalized records ingestion 0. ctest -L integration: orchestrator + gui GREEN (6.15s, 신규 hex TC 포함), cli 16/17 (1 RED 동일 pre-existing). 7 files +180/-1. | cycle-20 (P2.F5 C3): viewport-driven lazy 행 생성 — QAbstractScrollArea scrollContentsBy / resizeEvent hook, fillRange(startRow, count), 보이는 ±200 buffer, scrollToAddr 가 visible window 안에 들어오도록 갱신 + gui_smoke (큰 binary 시뮬은 elf_smoke 17KB 안에서 검증). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.10 (P2.F5) cycle-18 landed — design grilling + ADR-0043 + Phase 11.3.9 close (8h auto-session)**. Phase 11.3.9 (P2.F4) closed — C1+C2+C3 (3 cycles + ADR) all GREEN, C4 polish 는 선택. P2.F5 entry. Plan sub-agent (multi-agent token-saving, 146s, 25 tool) 으로 5 design 결정 grilling: D1 데이터 source = **mmap 직접 read (display-only path)** — rizin pxj 거부 (분석 채널 오용 + JSON 비용), `MappedFile` 인프라 재사용, **raw bytes 는 R-9/R-12 의 "분석" 정의 어디에도 해당 X — F2/F3 의 rizin 강제 자연 완화** (핵심 통찰), D2 **CLI 추가 X** (`xxd` 동치, F2/F3 와 비대칭은 "분석 vs 분석 아님" 의 자연 결과), D3 GUI = QPlainTextEdit monospace (DisasmPane 패턴 답습 — `<addr16>  <16 byte hex>  <ASCII>`), D4 scroll = mmap 전체 + viewport-driven lazy 행 생성 (±200 rows buffer, 100MB+ 지원 보장 — PRD §1 "Cutter/IDA 레벨" 약속 일관성), D5 cross-pane = **단방향 in 만** (disasm.instructionSelected → hex.scrollToAddr, cfg.blockActivated → hex.scrollToAddr) — reverse (hex click → disasm) 는 nearest-instruction snap edge case 로 v2 deferred. F5 cycle 분해: C1 ADR (이번) + 11.3.9 close, C2 HexPane skeleton + MappedFile + 기본 렌더, C3 viewport lazy, C4 cross-pane sync. **R-12 가드 핵심 명시**: hex bytes = `AuraInstructionRecord.bytes` 와 동일 데이터일 수 있으나 hex pane 은 **record 거치지 않고 직접 mmap read** — Display-only path, `AuraEngineResponse.body` 에 신규 type 추가 0, normalized records ingestion 0. 2 files +130 (ADR + ROADMAP, 코드 변경 0). | cycle-19 (P2.F5 C2): HexPane class 신규 (QPlainTextEdit + MappedFile binding + 16 byte/row format) + main_window 의 hexDock + tabify + CMakeLists 신규 cpp + gui_smoke 첫 행 ELF magic 검증 ("7f454c46  ...  .ELF..."). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.9 (P2.F4) cycle-17 landed — function nav history (Alt+Left/Right) (8h auto-session)**. P2.F4 C3 per ADR-0042 D3+D4. `src/gui/main_window.h`: QStack<quint64> m_navBack/m_navForward + bool m_navInProgress + m_actNavBack/Forward + canGoBack/canGoForward + goBack/goForward public slots. `src/gui/main_window.cpp`: View 메뉴에 Back/Forward QAction (Alt+Left/Right shortcut, browser-standard) — toolbar 신설 거부 (ADR-0042 D4). onFunctionRowSelected: m_navInProgress flag false 일 때만 m_navBack push + m_navForward.clear (browser non-linear nav 표준). goBack/goForward: pop one, push other, m_navInProgress=true 후 m_functionTable->selectRow → 정상 select 흐름 통과 + onFunctionRowSelected 가 stack push skip, 마지막에 false. action enabled 상태 동기화. Helper rowOfFunctionByEntry (anonymous namespace). `tests/integration/gui_smoke/gui_smoke.cpp`: 신규 SUBCASE — function 0→1 selectRow 후 canGoBack=true/Forward=false, goBack 후 canGoBack=false/Forward=true, goForward 후 다시 toggle. ctest -L integration: orchestrator + gui GREEN (5.65s), cli 16/17 (1 RED 동일 pre-existing). **mid-cycle fix**: 첫 build LNK error — QStack 가 main_window.h 에서 forward decl 만 됐을 때 멤버로 선언하면 incomplete type. include <QStack> 추가 후 GREEN. 4 files +95/-1. | cycle-18 (P2.F4 C4 close): polish + R-5 가드 회귀 — gui_smoke 부트스트랩 timing 안정화 + override_store integration TC 재실행 검증 + Phase 11.3.9 (P2.F4) close. 그 후 P2.F5 (Hex view) 진입. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.9 (P2.F4) cycle-16 landed — DecompilePane context menu 5 actions (8h auto-session)**. P2.F4 C2 per ADR-0042 D2. `src/gui/decompile_pane.{h,cpp}`: setContextMenuPolicy(CustomContextMenu) + customContextMenuRequested → onContextMenuRequested slot → QMenu 5 actions (Jump to disasm / Rename function / Reset name / Find xrefs / Add comment), 5 신호 emit (contextJumpToDisasm/Rename/ResetName/FindXrefs/AddComment) — addr 가 0 (line_map 미매핑) 일 때 addr-의존 액션 disable. `src/gui/main_window.{h,cpp}`: 5 slot 구현 + connect — Jump = disasm.selectInstruction (재활용), Rename/Reset = active function entry → buildKeyForFunction + override_store_put RENAME / store_delete (기존 renameFunctionAt/resetFunctionNameAt 재활용), FindXrefs = m_xrefsDock raise (v1; addr 별 필터는 F4 v2), AddComment = active function 단위 ANNOTATION payload (instruction-level annotation 은 F4 v2 — 현재 store key 가 function-scoped). `m_activeFunctionAddr` 멤버 + onFunctionRowSelected 갱신. R-5 가드 명문화 (Rename/Comment 모두 store SQLite 누적, AuraEngineResponse mutate 0). **schema bump 0** — Phase 4B 의 `AURA_OVERRIDE_PAYLOAD_ANNOTATION` 재사용 (Plan agent 가 grilling 중 발견한 자산). ctest -L integration: orchestrator + gui GREEN (5.70s), cli 16/17 (1 RED 동일 pre-existing). 4 files +160/-1. | cycle-17 (P2.F4 C3): nav history — QStack<quint64> back/forward in MainWindow + goBack/goForward slot + View 메뉴 actions + Alt+Left/Alt+Right shortcut + onFunctionRowSelected push + gui_smoke A→B→A→Back / Forward 시나리오. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.9 (P2.F4) cycle-15 landed — bidirectional line_map sync (8h auto-session)**. P2.F4 C1 per ADR-0042 D1. F2 의 deferred 역방향 sync 완성 (ADR-0040 D5 의 // TODO F4). `src/gui/disasm_pane.{h,cpp}`: `instructionSelected(quint64 addr)` signal 추가, m_lineToAddr (vector<quint64> by 0-based line) 채우기 in showDisasm, `onCursorPositionChanged()` slot — blockNumber 로 addr 해상 후 emit; `selectInstruction()` 안에 `QSignalBlocker(m_textView)` 추가 (F4 mutual sync echo 방지). `src/gui/decompile_pane.{h,cpp}`: `selectLine(quint64 addr)` public slot 추가, m_currentAddrToLine (역인덱스 — 같은 addr 가 여러 line 매핑 시 작은 line 우선) 빌드 in showDecompile, slot 안 QSignalBlocker. `src/gui/main_window.cpp`: `connect(disasmPane.instructionSelected → decompilePane.selectLine)` 추가 (mediator pattern, ADR-0041 cross-pane 라우팅과 동형). `tests/integration/gui_smoke/gui_smoke.cpp`: 신규 SUBCASE — disasm 첫 line 의 hex addr 파싱 후 selectInstruction(addr) 호출, echo loop 방지 검증 (no crash + no infinite loop). ctest -L integration: orchestrator + gui GREEN (5.69s), cli 16/17 (1 RED 동일 pre-existing). 5 files +95/-3. | cycle-16 (P2.F4 C2): 우클릭 context menu (Jump/Rename/Reset/Xrefs/Comment 5 actions) — DecompilePane 의 customContextMenuRequested + QMenu + override_store put RENAME/ANNOTATION + xrefs dock 라우팅. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.9 (P2.F4) cycle-14 landed — design grilling + ADR-0042 (8h auto-session)**. P2.F4 entry. Plan sub-agent (multi-agent token-saving, 85s, 13 tool) 으로 5 design 결정 grilling. **핵심 발견**: `override_store.h` 에 `AURA_OVERRIDE_PAYLOAD_ANNOTATION` 이 Phase 4B 부터 이미 등재됨 → comment 추가에 schema bump 0 (sub-agent 가 R-5 가드 검증과 동시에 backend 자산 발굴). D1 양방향 sync = DisasmPane.instructionSelected signal → DecompilePane.selectLine slot, QSignalBlocker re-entrancy guard (mutual echo 방지), main_window 단일 mediator. D2 컨텍스트 메뉴 5 actions (Jump/Rename/Reset/Xrefs/Comment) v1 포함, DisasmPane 미러는 F4 v2 deferred. D3 nav history = QStack<quint64> back/forward (entry 만, cursor pos 는 v2 additive). D4 UI = Alt+Left/Right + View 메뉴, toolbar 신설 X (ADR-0036 직관 보존). D5 cycle 분해 = ADR (이번) + bidirectional sync + context menu (5 actions, ANNOTATION 포함) + nav history + polish. **R-5 가드 명시**: Rename/Comment 는 override_store SQLite 누적, AuraEngineResponse mutate 0. 1 file +153 (ADR only, 코드 변경 0). | cycle-15 (P2.F4 C1): bidirectional sync 구현 — disasm_pane instructionSelected signal + decompile_pane selectLine slot + addr→line 역인덱스 + main_window 양방향 connect + QSignalBlocker + gui_smoke. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.8 (P2.F3) cycle-13 landed — pan/zoom + double-click cross-pane sync; F3 closed (8h auto-session)**. P2.F3 C4 close-out per ADR-0041 D5. CfgPane interaction: setDragMode(ScrollHandDrag) (좌클릭 빈 공간 드래그 = pan), wheelEvent override (Ctrl+wheel only — 실수 스크롤 zoom 방지), mouseDoubleClickEvent override (itemAt + dynamic_cast<BasicBlockNode> 부모 traversal 후 startAddr 추출 → emit blockActivated). 신규 signal `blockActivated(quint64 addr)`. main_window 에 connect (cfgPane.blockActivated → disasmPane.selectInstruction) — F2 의 selectInstruction slot 재활용 cross-pane sync. test hook `activateBlockAt(int index)` (scene items Ascending order traversal + index 번째 BasicBlockNode signal 강제 트리거). gui_smoke 신규 SUBCASE: cfgDock + disasmDock 양쪽 cast → 함수 walk 해서 ≥1 block 있는 함수 찾고 (PE fixture 한계 케이스에서는 graceful skip), activateBlockAt(0) 으로 signal trigger 검증. ctest -L integration: orchestrator + gui GREEN (5.66s), cli 16/17 (1 RED 동일 pre-existing). 3 files +120/-1. **Phase 11.3.8 (P2.F3) closed**. | cycle-14 (P2.F4 entry): F4 (Decomp pane 강화) 진입 — 양방향 line_map sync (disasm cursor change → decomp scroll-to + highlight, F2 의 한쪽 방향 보완), 우클릭 컨텍스트 메뉴 (Rename / Find xrefs / Add comment / Jump to disasm), 함수 navigation history (back/forward). design grilling 필요 (Plan sub-agent). | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.8 (P2.F3) cycle-12 landed — BasicBlockNode + EdgeArrow + BFS layout (8h auto-session)**. P2.F3 C3 per ADR-0041. CfgPane 가 텍스트 summary → 실제 그래프 렌더링으로 진화. 신규 클래스 `BasicBlockNode : QGraphicsRectItem` (220×56px, 어두운 배경 #282c34 + monospace 라벨: addr, block#, byte size — Consolas pt9), `EdgeArrow : QGraphicsPathItem` (color map per ADR-0041 D5: branch_true=green#4caf50, branch_false=red#f44336, call=blue#2196f3, fallthrough/jump/unknown=gray, return=purple#9c27b0; 화살촉 = 끝점에서 unit-vector 기반 두 짧은 segment). **BFS topological layout (Sugiyama 단순화 per D4)**: entry 블록 = startAddr==funcAddr 매칭 (없으면 lowest startAddr fallback) → outgoing-edge adjacency 으로 depth 할당 (QQueue<quint32>) → unreachable 블록은 maxDepth+1 → depth 별 row 그룹 + startAddr 정렬 (deterministic) → row 별 width 계산해서 좌→우 균등 배치. **R-9 가드 명시 in 헤더 주석**: layout 은 GUI presentation, 분석 X. PE fixture 의 0 blocks 케이스도 placeholder 로 graceful (sceneItemCount ≥ 1 보장). gui_smoke 신규 SUBCASE: cfgDock objectName + CfgPane qobject_cast + sceneItemCount ≥ 1 검증. ctest -L integration: orchestrator + gui GREEN (8.57s), cli 16/17 (1 RED 동일 pre-existing). 3 files +175/-15. | cycle-13 (P2.F3 C4 close-out): pan (ScrollHandDrag) + zoom (Ctrl+wheel) + mouseDoubleClickEvent → emit blockActivated(addr) → main_window connect → DisasmPane.selectInstruction. cross-pane sync (CFG → Disasm). gui_smoke 시뮬레이션. F3 종료 후 P2.F4 (Decomp pane 강화) 진입. | (계속) (1) cli_smoke override pre-existing. (2) PE backend block/edge 0. |
| 2026-05-04 | **Phase 11.3.8 (P2.F3) cycle-11 landed — CLI cfg + RPC cfg_function + CfgPane skeleton (8h auto-session)**. P2.F3 C2 per ADR-0041. ADR D1 핵심 구현: data ownership = analyze body (rizin agfj 재호출 X, 새 body type X, m_blocks/m_edges 가 analyze 직후 mirror, RPC/CLI 가 function_id 필터). 신규 `src/gui/cfg_pane.{h,cpp}` (QGraphicsView 기반 skeleton — C2 단계는 텍스트 summary 만, 노드/엣지 렌더는 C3), `src/gui/main_window.h` (GuiBlockRecord/GuiEdgeRecord struct + CfgPane forward decl + m_cfgDock/m_cfgPane + blocksForFunction/edgesForBlocks accessor), `src/gui/main_window.cpp` (cfgDock 셋업 + tabify decompile/disasm/cfg 3-tab 그룹, analyze post-step 에서 m_blocks/m_edges mirror + AURA_EDGE_KIND 매핑, function row select 시 cfgPane.showCfg 자동 트리거, accessor 구현 — function_id 매칭 + 블록 set membership 으로 edge 필터, clearCache cfg 도 포함, QSet include), `src/gui/rpc_server.cpp` (`cfg_function(addr)` method — blocksForFunction + edgesForBlocks → JSON {blocks, edges}), `src/cli/main.cpp` (cmd_cfg subcommand + `--func <hex>` + run_cfg 함수 — analyze 1회 호출 + body 의 blocks/edges 를 function_id 로 필터하여 JSON {blocks, edges, function_id} emit), `src/gui/CMakeLists.txt` + `tests/integration/gui_smoke/CMakeLists.txt` 신규 cfg_pane source, `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TDD TC `cfg --func emits blocks+edges body` (analyze entry 발견 → cfg 호출 → body 형식 검증 + canonical record shape). **Mid-cycle finding (격리 기록)**: PE fixture (Windows path) 의 analyze 가 419 functions 발견하지만 BlockRecord/EdgeRecord 0 (rizin agfj 처리 path 미동작). ELF path 는 정상 (13 fn / 24 blk / 14 edge). Phase 2A backend 한계, 별도 fix backlog. TC 는 array shape 만 검증하도록 완화 (≥1 강제 거부). 9 files +280/-1. ctest -L integration: orchestrator + gui GREEN, cli 16/17 (신규 cfg TC PASS, 1 RED 동일 pre-existing override). | cycle-12 (P2.F3 C3): BasicBlockNode QGraphicsItem + EdgeArrow QGraphicsPathItem + BFS topological layout + EdgeKind color map + gui_smoke (cfgDock objectName + scene items count == blocks+edges). | (계속) (1) cli_smoke override pre-existing fail. (2) PE backend BlockRecord/EdgeRecord 0 — Phase 2A 한계 backlog. |
| 2026-05-04 | **Phase 11.3.8 (P2.F3) cycle-10 landed — design grilling + ADR-0041 (8h auto-session)**. P2.F3 entry. Plan sub-agent (multi-agent token-saving, 144s, 24 tool) 으로 5 design 결정 grilling: D1 데이터 fetch = on-demand RPC `cfg_function(addr)` 단 데이터 ownership 은 **analyze body 재사용** (R-12 single-source-of-truth — 새 body type 거부, agfj 재호출 거부 — F2 와 핵심 차이), D2 CLI = `aura cfg --func <addr>` JSON {blocks, edges} (DisasmCLI 답습), D3 GUI = QGraphicsScene + QGraphicsView + custom QGraphicsItem (graphviz 의존성 도입은 §5 STOP), D4 Auto layout = BFS topological + 동일 depth 가로 배치 (Sugiyama 단순화) — **R-9 가드 명시**: layout 은 GUI 표시이지 분석 X, D5 edge color (true=green/false=red/call=blue/...), straight line v1, ScrollHandDrag pan, Ctrl+wheel zoom, double-click block → `DisasmPane::selectInstruction(block.start_addr)` (ADR-0040 기존 slot 재활용 — cross-pane sync). F3 cycle 분해: C1 ADR (이번), C2 CLI+RPC+CfgPane skeleton, C3 BasicBlockNode+EdgeArrow+BFS, C4 pan/zoom+double-click+gui_smoke, C5 (deferred F3 v2) Sugiyama 정식 또는 orthogonal routing. 1 file +132 (ADR only, 코드 변경 0). | cycle-11 (P2.F3 C2): CLI cmd_cfg + RPC cfg_function (analyze body filter by function_id) + cfg_pane.{h,cpp} skeleton + main_window cfgDock + tabify disasmDock + cli_smoke TC. | (계속) cli_smoke pre-existing baseline fail. |
| 2026-05-04 | **Phase 11.3.7 (P2.F2) cycle-9 landed — line_map sync; F2 closed (8h auto-session)**. P2.F2 C4 close-out per ADR-0040 D5. 변경: `src/gui/decompile_pane.h` (DecompileLineAddrMap typedef + Entry struct 에 lineMap 추가 + 2 cache overload + cachedLineMap accessor + lineSelected signal + onCursorPositionChanged slot), `src/gui/decompile_pane.cpp` (cursor connect 생성자, blockNumber+1 → lineMap lookup → emit, m_currentLineMap 스냅샷), `src/gui/disasm_pane.h` (selectInstruction(quint64 addr) public slot + m_addrToLine 내부 hash + currentCursorLine test hook), `src/gui/disasm_pane.cpp` (showDisasm 가 m_addrToLine 채움, selectInstruction 가 hash → block → setTextCursor + ensureCursorVisible + ExtraSelection 형광 highlight), `src/gui/main_window.cpp` (runDecompile 가 aura_rizin_decompile_body_line_map 추출 → DecompileLineAddrMap 빌드 → DecompilePane.cache 신 overload, 배치 decompile 도 동일, buildCentralStack 끝에 connect(decomp.lineSelected → disasm.selectInstruction)), `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE (TDD: disasmPane->selectInstruction(parsed addr from line 1) → currentCursorLine == 1 검증; fixture line_map 의존성 회피 위해 직접 slot 호출). 역방향 (disasm→decomp) 은 F4 backlog (ADR-0040 D5 명시). ctest -L integration: orchestrator + gui GREEN, cli 15/16 (1 RED 동일 pre-existing). 6 files +152/-7. **Phase 11.3.7 (P2.F2) closed**. | cycle-10 (P2.F3 entry): F3 CFG view 진입. design grilling (Plan sub-agent) 으로 InstructionRecord 와 같은 다섯 design 결정 정리 — BlockRecord/EdgeRecord 재활용 (이미 R-12), QGraphicsScene vs custom widget, edge routing (true/false/call 색상), pan/zoom UX, cycle 분해. 그 후 ADR-0041 작성. | (계속) cli_smoke pre-existing baseline fail (별도 fix backlog). |
| 2026-05-04 | **Phase 11.3.7 (P2.F2) cycle-8 landed — DisasmPane GUI + RPC disasm_function (8h auto-session)**. P2.F2 C3 per ADR-0040. 신규 `src/gui/disasm_pane.{h,cpp}` (QPlainTextEdit monospace + per-funcAddr cache QHash<quint64, QVector<GuiInstructionRecord>>, line format `addr  bytes  mnemonic op_str`, decompile_pane 패턴 답습), `src/gui/main_window.h` (GuiInstructionRecord struct + DisasmPane forward decl + m_disasmDock/m_disasmPane 멤버 + public runDisasm/disasmList/disassembleFunctionAt 메서드), `src/gui/main_window.cpp` (Disasm dock 셋업 + tabify decompile dock per ADR-0040 D5, runDisasm 함수 — orchestrator dispatch + cache fill + GuiInstructionRecord 변환, onFunctionRowSelected 가 runDecompile + runDisasm 둘 다 호출, clearCache 양 pane), `src/gui/rpc_server.cpp` (`disasm_function(addr)` method — runDisasm + disasmList 조합, JSON 응답 instructions[] array), `src/gui/CMakeLists.txt` + `tests/integration/gui_smoke/CMakeLists.txt` 신규 source, `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE (disasmDock objectName + disassembleFunctionAt(0) + 텍스트 non-empty + blockCount ≥ 1). **빌드 fix mid-cycle**: rpc_server.cpp 의 `req` 변수명을 기존 핸들러 컨벤션 `params` 로 수정 (cycle 내 self-correct, commit 전). ctest -L integration: orchestrator + gui GREEN, cli 15/16 (1 RED 동일 pre-existing). 8 files +220/-2. | cycle-9 (P2.F2 C4 — F2 close-out): line_map one-way sync (decomp click → disasm select), DecompilePane lineSelected signal + DisasmPane selectInstruction slot + main_window connect + gui_smoke 시나리오 (시뮬레이션 라인 클릭 → disasm cursor 위치 검증). | (계속) cli_smoke pre-existing baseline fail. |
| 2026-05-04 | **Phase 11.3.7 (P2.F2) cycle-7 landed — pdfj parsing + CLI `aura disasm` (8h auto-session)**. P2.F2 C2 per ADR-0040. 신규 `src/adapter/rizin/rizin_disasm_normalize.{h,c}` (pdfj cJSON → AuraRizinDisasmBody single-blob alloc, R-11 provenance, opcode→mnemonic+op_str split — 단순 whitespace slice no semantic interpretation), `src/adapter/rizin/rizin_command_set.{h,c}` `aura_rizin_cmd_build_disasm(addr_hex)` (aaa+pdfj framed bulk, build_decompile 패턴 답습), `src/adapter/rizin/rizin_adapter.c` (manifest supported_types 에 DISASM 비트 추가, handle_disasm() 신설 — handle_decompile JSON path 답습, dispatch case AURA_ENGINE_REQ_DISASM 추가), `CMakeLists.txt` rizin_disasm_normalize.c 추가, `src/cli/main.cpp` (disasm subcommand + disasm_body_to_json + run_disasm), `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TDD TC (analyze 로 entry 발견 → disasm 호출 → body version=1 + instructions[] count==array length + canonical record shape + provenance.source=="rizin"). **Sub-agent recon (multi-agent token saving)**: cycle-7 위치 9개 (engine_request DISASM enum 이미 존재, manifest line, dispatch line, command_set 패턴, decompile_normalize 패턴, CMake line, CLI subcommand 위치, dispatch 위치, cli_smoke helper) 를 Explore agent 가 72s 23 tool 으로 정확히 mapping → main 600단어 summary 만 받음. ctest -L integration: orchestrator + gui GREEN, cli_smoke 15/16 (신규 disasm TC PASS, 1 RED 동일 pre-existing). 7 files +280/-3. | cycle-8 (P2.F2 C3): RPC `disasm_function` + GUI Disasm dock (DisasmPane 신규 class, monospace QPlainTextEdit) + tabify Decompile dock + 함수 더블클릭 → disasm_function + 캐시 + gui_smoke TC. | (계속) cli_smoke pre-existing baseline fail. |
| 2026-05-04 | **Phase 11.3.7 (P2.F2) cycle-6 landed — InstructionRecord + DisasmBody header (8h auto-session)**. P2.F2 C1 per ADR-0040. 신규 `AuraInstructionRecord` (10번째 1급 record per R-12: provenance/function_id/addr/size/bytes[24]/mnemonic[16]/op_str[96]/type[16]/jump/fail; addr 가 자연 키, 별도 instruction_id 없음). `include/aura/normalized_records.h` (struct + _Static_assert + add/count/at API), `src/core/normalized_records.c` (instructions AuraRecVec + DEFINE_ADD/COUNT_AT 패턴 답습). `include/rizin_adapter.h` (`AuraRizinDisasmBody` magic 0xAFAA2C01u version 1u: header + function_addr + instructions_count, single-blob layout per ADR-0040 D4), `src/adapter/rizin/rizin_adapter.c` (accessor `aura_rizin_disasm_body_instructions` returns NULL for invalid magic / empty body). cycle-1 (StringRecord) 패턴 답습. ctest -L integration: orchestrator + gui GREEN, cli 14/15 (1 pre-existing). 4 files +52/-1. | cycle-7 (P2.F2 C2): rizin pdfj parsing + `aura --disasm --func` CLI + DISASM dispatch in adapter. | (계속) cli_smoke pre-existing baseline fail. |
| 2026-05-04 | **Phase 11.3.7 (P2.F2) cycle-5 landed — design grilling + ADR-0040 (8h auto-session)**. P2 entry. Plan sub-agent (multi-agent token-saving) 으로 5 개 design 결정 grilling: D1 InstructionRecord (10번째 1급 record, structured fields — mnemonic/op_str/bytes/jump/fail; pdj raw 활용 R-9 준수), D2 CLI scope (`--func <addr>` only, range 는 v2 deferred), D3 RPC `disasm_function(func_addr)` (1함수=1호출=1 cache key, decompile pattern 답습), D4 별도 `AuraRizinDisasmBody` (magic 0xAFAA2C01 v1, analyze body bump 거부 — Quick/Full split 보호), D5 GUI Disasm dock + Decompile dock tabify + line_map 편도 sync (decomp→disasm only, 역방향 F4 deferred). F2 cycle 분해: C1 데이터 모델, C2 pdfj parsing + CLI, C3 RPC + GUI dock, C4 line_map sync. **R-1/R-9/R-10 가드 명시**: AURA 가 자체 disasm 안 함, `pdfj` JSON 만, RzCore leak 0. 1 file +132 (ADR only). | cycle-6 (P2.F2 C1): InstructionRecord 정의 + DisasmBody header + accessor + R-11 _Static_assert + collection unit test (TDD strict). | (계속) cli_smoke pre-existing baseline fail. |
| 2026-05-04 | **Phase 11.3.6 cycle-4 landed — Phase 11.3.6 closed (8h auto-session)**. Strings dock end-to-end. 신규 `src/gui/string_table_model.{h,cpp}` (QAbstractTableModel, 6 cols: Addr/Length/Encoding/Section/Content/Source), `src/gui/main_window.h` GuiStringRecord struct + stringList() 접근자 + m_stringsDock/Table/Model 멤버, `src/gui/main_window.cpp` Strings dock 셋업 (Symbols/Imports 와 tabify) + analyze 후 데이터 population (aura_rizin_analyze_body_strings), `src/gui/rpc_server.cpp` `list_strings` 메서드 (string_id/addr/length/encoding/section/content/source), `tests/integration/gui_smoke/gui_smoke.cpp` 신규 SUBCASE (stringsDock objectName + columnCount==6 + rowCount==stringList.size + R-11 provenance). `src/gui/CMakeLists.txt` + `tests/integration/gui_smoke/CMakeLists.txt` 신규 source 추가. **TDD strict**: gui_smoke TC RED 먼저 (compile fail) → 구현 GREEN. fixture (elf_smoke.x86_64, minimal stub) 가 .rodata literal 0 이라 TC 는 strs.size() ≥ 0 + 모델/도크 contract 검증. **multi-agent**: cycle-4 패턴 탐색은 Explore sub-agent 위임 (commit 03a5d2e8/c39f3683 분석, 600단어 summary 만 main 에 들어옴 → 토큰 절약). ctest -L integration: orchestrator + gui GREEN, cli 14/15 (1 pre-existing). 8 files +175/-0. | cycle-5 (P2 entry): F2 Disasm view 진입 전 `/grill-me` design grilling 라운드 (InstructionRecord schema, scope, line_map 통합). | (계속) cli_smoke pre-existing fail. |
| 2026-05-04 | **Phase 11.3.6 cycle-3 landed (8h auto-session)** — CLI `analyze` body now emits `strings[]` array (mirroring Phase 11.3.5 cycle-1 xrefs[] pattern). `src/cli/main.cpp` `analyze_body_to_json` 갱신: `strings_count` 추가 + `strings[]` array (string_id/addr/length/encoding/section/content/provenance). `tests/integration/cli_smoke/cli_smoke.cpp` 신규 TC `analyze body exposes strings[] array` (body version≥3 + array length == strings_count + canonical record shape + provenance.source=="rizin"). ctest -L integration: orchestrator_smoke + gui_smoke GREEN; cli_smoke 15 TC 중 14 GREEN (신규 strings TC PASS, 회귀 0; 1 fail 은 동일 pre-existing override-on-empty TC). 2 files +88/-0. | cycle-4: RPC `list_strings` method + GUI Strings dock + gui_smoke TC. 11.3.5 cycle-7 (Imports dock) 패턴 답습. | (계속) cli_smoke `override get on empty store` pre-existing fail. |
| 2026-05-04 | **Phase 11.3.6 cycle-2 landed (8h auto-session)** — rizin izj → AuraStringRecord 3-layer plumbing: `include/third_party_hub/rizin_canonical.h` (AuraRizinCanonString + canonical.strings/_count), `src/adapter/rizin/rizin_snapshot.c` (populate_strings + dispatch + flatvec lifecycle + free), `src/third_party_hub/rizin_to_aura.{c,h}` (emit_strings + STRING confidence/completeness 매크로). encoding 매핑: ascii/utf8/utf16le/utf16be/wide → AURA_STRING_ENC_*. R-1/R-9 준수 (engine bulk JSON 만 사용). 4 files +104/-5. ctest 회귀 0. | cycle-3: CLI analyze body 의 strings[] 배열 emitter | (계속) cli_smoke pre-existing fail. |
| 2026-05-04 | **Phase 11.3.6 cycle-1 landed (8h auto-session #1)** — AuraStringRecord (9th 1급 record per R-12) + body schema v2→v3 bump (stub strings_count=0) + ADR-0039. 변경: `include/aura/normalized_records.h` (AuraStringId / AuraStringEncoding / AuraStringRecord + alloc/add/count/at API + _Static_assert), `src/core/normalized_records.c` (strings AuraRecVec + DEFINE_ADD/COUNT_AT 패턴 답습), `include/rizin_adapter.h` (AURA_RIZIN_ANALYZE_BODY_VERSION 2→3 + strings_count + accessor), `src/adapter/rizin/rizin_adapter.c` (BODY_OFF_STRINGS macro + accessor + flatten_collection 갱신). ctest -L integration: orchestrator_smoke 5.08s GREEN + gui_smoke 5.85s GREEN + cli_smoke 8.25s 13/14 (1 TC RED). **회귀 검증**: `git stash` 로 cycle-1 변경 빼고 동일 ctest → cli_smoke 동일 1 TC fail (override get on empty store → analyze_for_get_failed) — pre-existing baseline bug, 회귀 0. | cycle-2: rizin izj 파싱 → AuraStringRecord 채우기 (rizin_to_aura.c) | **PRE-EXISTING (격리 처리)**: cli_smoke `override get on empty store returns not_found exit 1` TC 가 당시 baseline 에서도 RED — `[ERROR] rizin_adapter: per-function subprocess failed, continuing` 6회 출력 후 analyze 실패 → "not_found" 대신 "analyze_for_get_failed" 반환. 이후 ADR-0052 로 신규 runtime baseline 은 Rizin 0.8.0 shared64 mandatory reference + rz-ghidra 0.8.0 preferred pseudo-C provider when installed 로 교체. |
| 2026-05-03 | **Phase 11.3 GUI v1 landed — Slice B~H**. Slice B: `src/gui/` scaffold + MainWindow + QStackedWidget + Qt6 CMake (`AURA_BUILD_GUI` opt-in, `find_package(Qt6 COMPONENTS Widgets)`). Slice C: `project_binaries` SQLite registry (idempotent CREATE TABLE IF NOT EXISTS, open/put/list/count/mark_analyzed/remove, backward-compat with CLI override DB). Slice D: Project view (`ProjectBinaryModel` 4 columns Path/SHA8/Size/LastAnalyzed) + File→New/Open/Add Binary 메뉴 + Analyze 버튼 + 더블클릭 dispatch. Slice E: `AuraAnalysisLevel` enum 추가 (FULL=0/QUICK=1, default 0 = backward-compat) + `AnalysisOptionsDialog` Quick/Full 슬라이더 + Rizin adapter `aura_rizin_cmd_build_analyze_global_with_level()` 분기 (`aa` vs `aaa`). Slice F: `FunctionTableModel` 4 columns (Name/Entry/Size/Source) + 분석 성공 시 `QStackedWidget` page 1 자동 전환 + `Esc`/View→Back to Project 단축키. Slice G: `tests/integration/gui_smoke/` (offscreen Qt platform, doctest 기반, full flow: openProject → addBinary → analyzeBinaryAt(FULL) → functionCount ≥ 1) — Linux WSL ctest 5.0s GREEN. Slice H: CI workflow 갱신 (Linux apt qt6-base-dev + libgl/libxkbcommon, Windows `jurplel/install-qt-action@v4` v6.7.2 + cache, 양 환경 `AURA_BUILD_GUI=ON` + gui_smoke target build + ctest -L integration). Linux Tier 1: orchestrator_smoke 19.52s + cli_smoke 19.88s + gui_smoke 5.00s = 44.4s GREEN. `aura-gui` binary 4.7MB Linux WSL 실행 검증 (QT_QPA_PLATFORM=offscreen, EXIT=124 timeout = 정상 시작). 8 commits (`2f798eb9`..`bff13407`). | Phase 11.3.1 (v2 작업: detail pane, override editor, recent projects, drag-drop, 분석 취소, 4단계 옵션) 또는 Phase 11.4.1 (패키징 검증) | Windows MSVC + Qt6 검증은 CI 첫 실행 후 결과 확인 필요 (jurplel/install-qt-action 캐싱 + MSVC 2022 빌드 시간 비용). |
| 2026-05-03 | **Phase 11.3 Slice A (재정의) — ADR-0035 작성 + ADR-0034 superseded**. 사용자 grilling 라운드 (Q1~Q13 in /compact 후 재진입) 결과 GUI v1 표면이 "함수 list view only" → **project-first 흐름** 으로 구조 변경. 결정: project = 단일 `.aura.db` (Phase 11.4 단위 그대로) / single MainWindow + QStackedWidget (page 0 Project, page 1 Functions) / Analysis Options dialog Quick(`aa`)/Full(`aaa`) 2단계 / `project_binaries` 테이블 IF NOT EXISTS 추가 / Add Binary 메뉴 only / Recent/drag-drop/cancel = v2 / status bar progress (modal 회피) / Esc = Back to Project / override 즉시 commit (no dirty). orchestrator API 확장 = ANALYZE 에 `analysis_level` enum (default FULL = backward-compat). 코드 변경 0, 문서만 (ADR-0035 신규 + ADR-0034 superseded 표기 + CONTEXT.md GUI 정의 갱신 + Project/Analysis level 어휘 추가 + ROADMAP). Slice 재구성: A (본 커밋) → B scaffold → C schema → D Project view → E Analysis dialog + ANALYZE level → F Function list + view 전환 → G gui_smoke → H CI. | Slice B — `src/gui/` scaffold + MainWindow + 빈 menu bar + CMake (`find_package(Qt6 COMPONENTS Widgets)`, target `aura-gui`) | — |
| 2026-05-03 | **Phase 11.1 완료 — CLI v1 (`analyze` / `info` / `llm-context`) GREEN**. Slice A~F 6 커밋 (`c6596be1`..`e61fa879`). CLI11 v2.6.2 vendored, src/cli/main.cpp + src/core/sha256.{h,c} (PD), tests/integration/cli_smoke/ (4 TC: --version, analyze body schema, info fingerprint 5필드, llm-context binary mode). docs/cli.md narrative + Failure modes 표. CI 갱신 (orchestrator-smoke.yml 양 job 에 cli_smoke 추가, `-L integration` 일괄 실행). WSL ctest GREEN: orchestrator_smoke 19.61s + cli_smoke 9.41s = 29s. Phase 9 close-out: issue 01~08 + 09 (CI runner) 모두 흡수, ✅ closed. **잔여 grilling**: G1~G10 점검 완료, Phase 11.3 진입 직전만 grilling 라운드 보류 (GUI 표면 정의는 11.2 결과 후가 정합). macOS = 개발 포기 (환경 부재, ADR-0030 Tier 3 wording 정합). | Phase 11.1.1 (engines + failure TC) → Phase 11.2 (LLM e2e cookbook) → Phase 11.3 진입 직전 grilling. | git push origin main: 13 커밋 origin 미동기화 + remote 미설정 (사용자 결정). |
| 2026-05-03 | **Phase 11.1 진입 — Frontend topology 합의 + 문서화**. `/grill-me` Q1~Q15 + `/grill-with-docs` D1~D7 로 CLI/GUI plan 고정. ADR-0033 (Frontend topology — co-equal CLI + GUI on shared `aura_core`, CLI first, GUI bootstrap = CLI v1 + LLM e2e 후) 작성. CONTEXT.md 갱신: Frontends 섹션 (Frontend / CLI / GUI), Engine routing 섹션 (Primary set ≠ Reference engine 분리), Flagged ambiguities 갱신 ("smoke" 디렉토리 컨벤션 허용 / "primary engine" 단수형 금지 / "CUI" 자연어만 허용). CLI v1 표면 = `analyze` / `info` / `llm-context` (3 명령), one-shot subcommand 스타일, JSON-default 출력 (`"aura_schema": 1`), CLI11 vendored, `src/cli/`, target `aura`, stateless, `aura_orchestrator_select_primary` 정책, `tests/integration/cli_smoke/` doctest popen+JSON parse. | Slice B (`analyze` 명령) — CLI11 vendor + `src/cli/` scaffolding + main + dispatch + JSON 직렬화 | — |
| 2026-05-03 | **Phase 10 orchestrator_smoke 게이트 도착 (slice 1~3 + 정책 ADR)**. 11-question grilling (`grill-with-docs`) 으로 plan 고정 → CONTEXT.md (용어 사전) + ADR-0031 (reference engine policy: Rizin mandatory, Ghidra/etc optional) + ADR-0032 (external tool acquisition pattern: `third_party/<tool>/manifest.json` + `scripts/fetch_external_tools.{sh,ps1}`, FetchContent 거부) 작성 (commit 6482d661). slice 1 (5de85fcf): doctest 2.4.11 vendor + 당시 Rizin 0.8 계열 manifest + fetch script (Linux + Windows; python probe + cygpath MSYS 변환). slice 2 (1ebdffa7): aura_smoke.c 단일 source + scripts/rebuild_fixtures.{sh,ps1} (gcc + MSVC vcvars env import) + 커밋된 prebuilt elf_smoke.x86_64 (17KB) + pe_smoke.x86_64.exe (140KB). slice 3 (이번 커밋): tests/integration/orchestrator_smoke/orchestrator_smoke.cpp (doctest 기반, 4 invariant 화이트박스: I1 functions≥1 / I2 entrypoint / I3 SQLite `:memory:` override rename round-trip / I4 unified model JSON handwritten schema). CMake 신규 옵션 `AURA_BUILD_TESTS` (default ON) — `AURA_ENABLE_RIZIN=ON` 시 등록. 헤더 보강: normalized_records.h 의 `_Static_assert` → C++ 호환 shim (C++ TU 에서 include 가능). WSL Ubuntu 24.04 + gcc 13.3 ctest GREEN (1/1, 21.99s, label `integration;reference_engine`). **Superseded runtime note**: ADR-0052 이후 신규 기준은 scripts 가 fetch 하는 Rizin 0.8.0 shared64 mandatory + 설치되어 있을 때만 쓰는 rz-ghidra 0.8.0 preferred pseudo-C provider 다. rz-ghidra 부재 시 AURA 는 deterministic install guidance/fallback 을 보여주며 pseudo-C 를 fabricate 하지 않는다. | (1) Windows MSVC 환경에서 동일 cmake/ctest 검증 — Tier 1 양립 확인. (2) CI workflow 에 fetch script + cmake -DAURA_ENABLE_RIZIN=ON + ctest 단계 wiring (현재 .github/workflows 미점검). (3) Ghidra optional 시나리오 추가 (별도 fixture 안 필요, 동일 fixture + Ghidra adapter 만 등록). | aura_llm 컴파일 검증은 별도 (libcurl 8.5.0 WSL 에서 자동 활성화 확인됨). |
| 2026-05-03 | **Phase 10 시작 — Legacy engine strip**. `feat/strip-legacy-engines` 브랜치 (worktree `../AURA-strip-legacy`) 에서 src/{disasm,parser,decompiler,symbolic,gui,collab}/ 전량 + src/core/ 의 in-house 파이프라인 잔재 (recursive_disasm/taint/cfg_metrics/noreturn/inline/flirt/binary_diff/xref/*_refs/extern_*/crypto_detect/yara_search/decompile_body_view/auto_bookmark/data_archive/embedded_media/net_proto/search/headless/plugin_loader/analyzer_registry/aura.c/pipeline.c) + src/cli/decompile_* + tests/* 전량 + plugins/ 삭제. ~50k LOC. CMakeLists.txt 재작성: aura_core 최소화 (orchestrator/IPC/override pipeline + ghidra/rizin adapter), aura/aura-gui/aura-server/example_plugin 타깃 제거, capstone/zydis/yara/tinyxml2 의존성 제거. Ghidra subprocess 래퍼 (ghidra_subprocess.c, ghidra_xml_normalize.c) 만 src/decompiler → src/adapter/ghidra/ 로 이전. mapped_file 도 src/parser → src/core/ 이전 (외부 도구 인프라). MSVC build-strip Debug GREEN (aura_core.lib + cjson.lib + sqlite3.lib). | 새 GUI 설계 (Qt6 유지) + CLI 진입점 재정의. 머지 전 main 머지 전략 결정 필요. | aura_llm 은 현재 호스트에 libcurl 부재로 컴파일 미검증 — Linux 또는 vcpkg curl 환경에서 재확인 필요 |
| 2026-05-03 | **issue 03b·04·05·06·07 모두 close** (1 sweep). 03b: 6 emit API _with_strbuf adapter + aura_strbuf_capture_emit shared bridge in aura_io.c (cd605dde, 717ff819 forward-decl fix). 04: GUI 2 callsite → aura::StrBuf RAII (a2e7da65). 05+06: AURA_WEAK macro + analyzer_init_stubs.c #ifndef _MSC_VER guard, MSVC LNK2005 0 (5b49524c). 07: MappedFile.fd G6 canonical + 10 test guard 제거 (d7dcad19). Linux full ctest GREEN. tests/decompiler/* + src/gui/* 의 open_memstream/fmemopen = 0. MSVC build-rel 의 LNK2005 다중정의 제거됨; LNK2019 잔여는 per-target source list 미비 — issue 09 영역. | issue 09 (Windows MSVC CI runner) — depends-on 02·03·04·06·07 모두 ✅ 충족 | — |
| 2026-05-03 | **issue 03a close** — tests/decompiler/* 의 ir_emit_pseudoc / hir_emit_pseudoc 콜사이트 13건 (9 파일) 의 open_memstream/fmemopen → AuraStrBuf 치환. 신규 `ir_emit_pseudoc_with_strbuf(IrFunc*, AuraStrBuf*)` helper (codegen.c, decompiler.h) — tmpfile+fread 기반 thin adapter, 본체 무수정. fmemopen 2건 (test_const_prop, test_dead_code) 도 같은 helper 로 흡수. hir_emit 2건은 기존 hir_emit_pseudoc_with_positions 로 직행. 1 commit: 585be690. 회귀 net: 22/22 GREEN (Linux), MSVC build-rel 11/12 (1 실패는 pre-existing issue 05 weak-symbol 문제). 03b spawn: dwarf/pdb/exc/sr emit API 12 callsite 후속 작업 | issue 03b 또는 issue 05 (WEAK_STUB) 진입 가능 | — |
| 2026-05-03 | **issue 02 close** — hir_emit.c emit_* + 양 공개 API → AuraStrBuf 마이그레이션 (issue 02 명세는 stale `aura_io_memstream_*` 전제였으므로 재명세 후 진행). emit_indent/emit_int_literal/try_emit_stack_var/emit_expr/emit_stmt/emit_local_decls 시그니처 전환, 104 fprintf + 3 ftell + 2 fwrite 치환, 신설 emit_pseudoc_into() 로 양 공개 API 단일 경로 합류, aura_emit_memstream_* static 삭제. 2 commits: fcf647a2 (RED locks: Slice 1 byte-equiv + Slice 2 positions equiv) · 5ca982ed (migration). 회귀 net: hir_emit 관련 16/16 + decompiler 라벨 9/9 + Slice 1+2 GREEN | issue 03 (tests/decompiler/* memstream 마이그레이션) 또는 issue 05 (WEAK_STUB) 병렬 진입 가능 | MSVC 빌드 검증은 issue 09 로 이연 |
| 2026-05-03 | **issue 01 close** — AuraStrBuf 도입 (Q1~Q9 design grilling 합의 → 145+145+494 LOC + 11/11 ctest GREEN). 4 commits: 87b1e232·59287353·8bcc56fe·7e5b1ca0 | issue 02 (hir_emit.c 의 `aura_emit_memstream_*` static → `aura_strbuf_*` 마이그레이션, 3 callsite) 또는 issue 05 (WEAK_STUB) 병렬 진입 가능 | 미커밋 DIFF G3·G4·G6·G7 잔존 — issue 02·07 의 baseline 으로 흡수 예정 |
| 2026-05-03 | Phase 1~4 재점검 + Phase 9 Platform Coverage 구조 도입 (Matt 워크플로우) + ADR-0030 Tier 정책 채택 (commit 92221de8) | issue 01 진입 | — |
| 2026-05-02 | Phase 4D rendering-time overrides + 4E.1 conservative resolve pass (commit b0c625c1) | Phase 4 close-out 또는 Phase 5 진입 | — |
| 2026-05-02 | Phase 4C overlay apply gate + view + status persistence (commit 58a9366a) | Phase 4D | — |
| 2026-05-02 | Phase 4B SQLite override store (commit c7f33953) | Phase 4C | — |
| 2026-05-01 | Phase 4A stable identity contract (commit 7cacd258) | Phase 4B | — |
| 2026-05-01 | Phase 3 close-out + rz-ghidra 0.8 계열 capability backlog 해소 (commit 28a0ea88). Superseded runtime note: ADR-0052 기준은 Rizin 0.8.0 shared64 mandatory reference + rz-ghidra 0.8.0 preferred pseudo-C provider when installed. | Phase 4 | — |

> **Phase 0~3 핸드오프**: git log 참조. 본 로그는 Phase 4 이후만 유지 (legacy/AUTOMATION_ROADMAP.md 는 v2.x 기록 보존용).

---

## 사용 규칙

1. **세션 시작 시**: 본 파일 + `Tasks.md` + `git log -5` 읽고 현재 상태 파악.
2. **세션 중**: issue 단위 작업. 완료 시 `.scratch/<feature>/issues/NN-*.md` 의 `Status:` 갱신.
3. **세션 종료 시**: 본 파일 핸드오프 로그에 1줄 추가 + 활성 Phase 의 issue 진행 상황 표 갱신.
4. **Phase 종료 시**: `Tasks.md` 의 해당 Phase status note 업데이트 + `docs/phases/phase-N-closeout.md` (선택) 작성. 본 파일의 활성 Phase 행을 ⏸️ → ✅ 로 전이.
