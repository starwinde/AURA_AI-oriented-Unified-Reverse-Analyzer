# Tasks Archive — v2.2.0 Phase G + Phase H

> **Status**: frozen. rules.md §11.5 에 따라 상태 전환 금지.
> **Archived**: 2026-04-20 Session #9.
> **Parent canonical**: [Tasks.md](../Tasks.md)
> **포함**: Phase G (parity_score 의미론 overhaul) / Phase H (Decompile depth 상향). Session #6, 2026-04-19, HEAD 711c4e8

---

## v2.2.0 Phase G: parity_score 의미론 overhaul `[x]` — Session #6, 2026-04-19

> **명명 주의**: 본 Phase "G" 는 **벤치 도구 재설계** 이며, 함수 탐지 `G1~G5` gate (§v1.1 V1.1-3, `src/symbolic/func_detect.c`) 와 **무관** 하다. G1~G5 gate 참조는 그대로 유지된다.
> **상세 설계**: `tests/benchmark/HYBRID_SCORER_DESIGN.md` (commit 0ca90b3, 331줄). **AUTOMATION_ROADMAP.md §Phase G** 참조.

**완료 기준 (4/4 충족):**
- [x] **G-1**: Hybrid scorer 설계 결정 — legacy parity_score.py 보존 + AST-diff overlay 가법 추가. C++ `tools/ast_diff/` (libclang C API). PRD §4.1 준수, Python clang 바인딩 거부 (commit 0ca90b3)
- [x] **G-2**: `tools/ast_diff/` C++17 binary (~620 LOC + ~150 LOC tests + standalone CMake). variable rename + C-3 flatten + M cast-and-deref → MemberAccess 정규화. parity_score.py `--semantic` flag + subprocess wiring + graceful fallback. 24/24 Python + 4/4 C++ tests GREEN (commits ec38d72, 6864fdc)
- [x] **G-3**: 3-binary 50-pair 샘플 측정 — cat ast_struct=9.0/10 ast_branch=6.24/10, ls 4.20/1.40, aura_self 5.76/1.66, parse OK ~90% (`tests/results/v2_2_0_step_g3/SUMMARY.md`, commit 0d52d01)
- [x] **G-4**: `gap_analyzer.sh --semantic` plumb-through + Phase G DoD 종결 (`AUTOMATION_ROADMAP.md` §Phase G `[x]`, commit 0d52d01, 75e50ac)

**범위 경계 준수**: `tests/benchmark/` + `tools/ast_diff/` 만. `src/decompiler/*` 무수정. AURA 빌드 0 영향. 새 인터프리터 의존성 0.

---

## v2.2.0 Phase H: Decompile depth 상향 `[x]` — Session #6, 2026-04-19

> **명명 주의**: 본 Phase "H" 는 **디컴파일러 출력 품질 상향** 이며, 기존 gate 약자와 무관.
> **상세 설계**: **AUTOMATION_ROADMAP.md §Phase H** 참조.

**완료 기준 (4/4 충족, TDD RED→GREEN):**
- [x] **H-1**: Constant-condition `if` elision at HIR emit (`src/decompiler/hir_emit.c` HIR_IF case). `if (CONST N) then else` → live branch only. Closes ls FUN_00105e10-class divergence (`if (1) { } else { } return;` → `return;`). test_const_cond_eliminated GREEN, test_if_else fixture 갱신 (commit c147972)
- [x] **H-2**: Homogeneous-stride struct → array promotion (`src/decompiler/struct_recovery.c sr_analyze()` post-pass). 4+ same-typed fields at strided offsets {0, S, 2S, …} → SrArray with elem_count=N. 새 API `sr_get_array_elem_count()`. test_homogeneous_stride_promoted_to_array GREEN. Codegen plumbing (struct→array emit) follow-up (commit c15bb99)
- [x] **H-3**: Loop classification end-to-end regression guards. loop_detect → InductionVar → LOOP_TYPE_FOR → hir_builder → HIR_FOR → emit `for (` chain TDD 검증. 인프라 이미 존재 — 3 신규 RED→GREEN test (test_hir_for_loop_counted, test_hir_dowhile_strict_classification, test_for_loop_emits_for_keyword) (commit 25bc3f2)
- [x] **H-4**: Pointer-width narrowing on parameters (`src/decompiler/hir_builder.c` post-pass). Per register-passed param scan IR LOADs, derive smallest pointee width, narrow textual type from `void*` / `int64_t*` to `int8_t*`/`int16_t*`/`int32_t*`. DWARF/symbolic names 보존. test_h4_param_pointer_width_narrows_to_int32 RED→GREEN (commit d15d77b)

**범위 경계 준수**: `src/decompiler/*` + 관련 tests/ 만. 측정은 Phase G overlay 사용. Step 9 sample (cat 21 funcs): 평균 legacy +1.05 / ast_total +0.95.

**Phase H DoD 판정**: 4/4 lever GREEN + ctest 127/127 × 2 회귀 0 + Step 9 real-binary delta non-zero. **충족**.

**v2.3.0 follow-up**:
- H-2 codegen plumbing (`type_constraint.c integrate_struct_recovery` + emit `T[N]` syntax)
- Step 9 full 3-binary measurement (cat 85 + ls 412 + aura_self 1205 paris)
- 의미론적으로 더 깊은 lever (예: pointer-aliasing-aware struct recovery, cross-function type propagation)

---

